# 版本变更记录 / Changelog

最新版本在最上面，向下是历史。每版的安装包在 `outputs/CoopBots-v<版本>.zip`，
构建与打包说明见 [README.md](README.md)。

---

## 0.36.1

**修复一幕 Boss 的卡死：Bot 不出牌也不结束回合**。安装包：`outputs/CoopBots-v0.36.1.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.6.2 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- **症状**：人类结束回合后，Bot 长时间既不出牌也不结束回合，战斗整个停住。
- **根因**：内核把某个药水定为计划首步并成功喝掉后，如果该 Bot 随即没牌可打，`BotRuntime` 会走「无牌可打就跳过内核轮询」的分支；而药水确认 `ConfirmedPotion` 只在 `Poll()` 开头清空，`Reset()` 又**必须**保留它（`Poll` 是「先存计划、再重置搜索」，在那里清掉就把刚做出的决定毁了）。于是那条**已经执行过的**药水计划被反复读出来，`EnqueueManualUse` 因药水已不在主人的腰带上抛异常，异常从 `Tick` 抛出，结束回合的代码永远走不到——外层 catch 只是每 2 秒重试一次，于是永久停住。
- **日志特征**：`Potion … has owner , but the owner's potion list does not contain it!` 每约 2 秒重复一次。其中 owner 显示为空是**假线索**：报错发生在 `UsePotionAction` 的构造函数里，`Player` 属性此时尚未赋值。
- **修复**：新增 `KernelCombatPlanner.DiscardConfirmation()`，在跳过轮询的分支显式丢弃上一 tick 的确认；再加两道兜底——用药前先按游戏自己的规则校验药水仍属于该玩家，并把入队包进 try/catch。现在任何一次用药失败只会导致「这一瓶不喝」，而不会再吞掉整个回合。
- **回归**：新增断言钉住该不变量——`Reset()` 必须保留尚未被读取的计划、`DiscardConfirmation()` 必须清掉它、已不在腰带上的药水必须被拒绝而不是抛异常。
- **依赖基线提高到 RitsuLib 0.6.2**：RitsuLib 0.6.2 把运行时拆成了多个程序集（游戏版本相关的 `STS2-RitsuLib` / `STS2-RitsuLib.Runtime` 放在 `compat/<游戏版本>/`，与版本无关的 Shared / Ui / Settings 放在 `shared/`），旧版工坊项里的 `lib/0.111.0` 目录已不存在。内核引用的 `HarmonyIl`、`IComputedDynamicVar` 等类型现在位于 `STS2-RitsuLib.Runtime`，因此本版按 0.6.2 重新接线编译，`mod_manifest` 的 `min_version` 与创意工坊说明同步改为 **0.6.2**——仍停在 0.5.20 的玩家必须先更新 RitsuLib，否则 Mod 会因缺少 `STS2-RitsuLib.Runtime` 而加载失败。
- **发布状态**：已出本地包 `outputs/CoopBots-v0.36.1.zip`；GitHub `imbalilang/STS2-Co-op_Bots` 与创意工坊（id 3800805592，好友可见）已同步。

## 0.36.0

**复盘计划续：A3 药水覆盖、B1 营火回血、B2 终局金币、B3 构筑评分、C1 搜索回退分类与机制风险、D1 持有伤害按可格挡性计价、D2 营火按下一场加权、D3 终局药水不再留底、E1 爬塔深度感知**。安装包：`outputs/CoopBots-v0.36.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 preview.50 起的全部改动。）

- **A3 内核可模拟的药水从 24/66 提升到 59/66**。此前只有 5 种药水有镜像，一瓶没有镜像的药水**没有任何分支能用**，于是槽位整局占满（上一场战败时三人合计还剩 6 瓶）。现在补上 `Speed`（+5 敏捷，且是 `SpeedPotionPower`——它派生自 `TemporaryDexterityPower`，回合末会正确归还）、`Gigantification`、`Ship in a Bottle`，并把其余**效果自洽**的药水统一接到上游那份已经写好的确定性药水表上——一份实现，两个入口，重导入时不会各自漂移。5 种以"选择"结算的药水（`Ashwater`/`DropletOfPrecognition`/`GamblersBrew`/`LiquidMemories`/`TouchOfInsanity`）**故意不接**：它们在上游表里是空 `break`，接进来等于"免费无效果"，比拒绝更糟。
- **药水变量名不再静默失败**。`DynamicVars` 里找不到变量时以前求和为 0，药水就被模拟成"免费无效果"；现在直接抛错并在日志里列出实际变量名。有一条覆盖断言（59/66 + 逐瓶"用了必须改变局面"）把这件事钉死。
- **"为什么不喝"现在可查**。对战小结新增 `unmodeled-potions=…`，直接列出全队持有但内核无法模拟的药水瓶名——以前只能从战绩存档里看到"剩了 6 瓶"。
- **B1 营火改为「按血线二选一」，一、二幕贪锻造**。对抗的复盘：一幕 4 个篝火 × 3 个 Bot = 12 次机会**强化 0 次**（`rest_site_choices` 全是 HEAL/MEND），而同行的真人每个篝火都锻造，只有他的牌组在长——这正是"一幕 Boss 打不过去"的直接来源。旧公式把"补多少血"和"锻造"放在同一把尺子上比，但回血分随伤口浮动、锻造分是写死的 16/10/6，血一低锻造分反而往下掉（<60% 掉到 10，<40% 掉到 6），交叉点落在约 75% 上限——几乎没有 Bot 站得到线上。
  - **血线**：`SmithHpFloor` 一、二幕 **25%**，三幕 **55%**，三幕最后一个篝火 **85%**。线以上营地归牌组，线以下归血量。守住线不靠调两侧分数（那守不住，两侧单位本就不同），而是把**错的一侧乘 0.22**，线本身是硬判定。
  - **不再治疗队友，除非有帐篷**。MEND 等于把自己的营地送出去、自己的牌组不成长。唯一"免费"的情况是 `MiniatureTent`：它的 `ShouldDisableRemainingRestSiteOptions` 对持有者返回 false，**持有者保留全部选项**，一次营地可以连续锻造 + Mend；其他人只能选一个。所以没帐篷时只救 **≤25% 血量**的濒死队友。
  - **锻造价值 = 牌组里最值得锻造的那张牌**。不再写死：`bestUpgrade × 2.5`，`bestUpgrade` 取牌组 `UpgradeValue` 的最大值。基础牌天然垫底（Strike+ 3.6、Defend+ 3.0，真牌 5–8），所以有核心牌时永远是核心牌胜出；具体锻哪张牌由 `BuildValue.UpgradeDelta` 排序决定，这部分不变。
- **B2 终局金币按"花不掉就不值钱"计价**。上一场战败时全队还剩 646 金。最后一幕通常不会再有商店，因此买下一件刚刚保本的货也比留着强：最后一幕的成交门槛降到 0.8×，同时取消"必须留 25 金"的保留规则。**只缩放门槛，不翻转符号**——会让牌组变差的购买依旧被拒。
- **B3 构筑评分按功能，不再按体积**。23–24 张时规模压力原本高达 40–45 分，**超过任何一张牌自身价值**：日志里 Blood Wall 以 -37.8 被跳过，而同一行的理由写着 `needs-block`。现在（1）规模压力上限 30（稀释本来就另算一遍）；（2）**补上牌组短板（挡格/输出/能力/AOE/抽牌/能量）的牌与"路线内"同享 40% 减免**——同一句话里说"缺挡格"又罚 40 分是不可能的；（3）敏捷类成长不再要求"挖掘出的伙伴牌"：敏捷乘的是牌组**已有**的挡格，实测 33.6 → 42.0；（4）`block-retain` 路线改名为 `block-dexterity` 并只由敏捷驱动——牌池里**没有任何一张牌同时具有 Retain 与挡格**（12 张 Retain 牌挡格全为 0），所以旧定义里"保留一张手牌"会被当成挡格成型。
- **C1 搜索回退分类与机制风险**。
  - **没出牌的三种原因分开统计**。此前只记一个 `no-action` 计数；复盘能数出"最终 Boss 61 次出牌里 40 次来自旧策略"，却说不出内核为什么弃权。现在按状态分成 `Idle`（真的无牌可打，无害）/ `Tie`（模拟过牌但都不如过牌，是评分缺口）/ `Boundary`（被未建模机制截断，是模型可以补的），进入对战小结的 `no-action-kinds=`，并且在紧随其后的那条出牌日志上标 `via=legacy:kernel-tie` 之类，让"哪一种弃权最终变成了哪一步行动"可对账。
  - **睡眠类怪招不再判负**。`ROCKET/RECHARGE_MOVE` 是一次纯 `SleepIntent` 的喘息（上游实机差分记录：只播重新充能表现、不改变任何战斗数值），但它不在逐怪白名单里，于是**整个敌方回合被判负**——同一场里出现 30 次，意味着有这只怪在时内核**完全无法跨回合模拟**。现在沿用意图预测器已有的规则：只含 Sleep/Stun/Hidden 的招式就是"什么都没做"，不需要逐怪登记。
  - **旧策略继承出牌计数风险**。内核会模拟"每出第 N 张牌生成一张状态牌"，而旧评分器一张一张地评牌，看不到"再多打一张就会塞给自己一张凋零"。现在 `StatusTriggerPenalty` 在计数器只剩 1 时，把那张状态牌的回合末代价从这一手的评分里扣掉（机制查询在核心里，评分只用结果），复现复盘里"临终把整手牌打进计数器、然后死在自己造出的凋零上"的场景。
- **D1 持有型回合末伤害按"能不能被格挡"分开计价**。Beckon 扣的是 `HpLoss`——**格挡挡不住的生命流失**；永世沙漏的 Wither 扣的是 `DamageVar`——**就是普通伤害，格挡照挡**。两者此前都落进同一个"不可格挡"桶里整额扣分，于是快速叶节点把 Wither 算得比实战更贵、Bot 会更用力地躲它。现在 `HeldPenalty` 拆成不可格挡（`HpLoss`，仍直接计成本）与可格挡（`Damage`，并入 `incoming` 与敌方攻击一起被格挡吸收）两部分，**仍然只有一份实现**。同时钉死：Wither 被 Aeonglass 的 `FakeUpgrade` 升级后必须更贵（3 → 6），否则里已升级、价按初版算。
- **D2 营火回血按"下一场是什么"加权**。回血公式此前只问"你伤得多重"，不看后面是 Boss 还是商店——复盘里三人就是以 43–61% 的血量走进 Boss 房的。现在按当前营地**可达的下一间房**加权：Boss 1.6、精英 1.35（第三幕 1.5）、普通怪 1.15、未知 1.1；后面还是营地/商店/宝箱则降到 0.85（那时还能再补）。只乘在回血权重上，不动曲线本身；读不到地图时一律 1.0。实测同一处 55/73 的伤：后面是商店 11.0（仍选升级），后面是 Boss 20.6（改选休息）。
- **D3 药水改为「价值兑现就喝」，不再只在濒死时喝**。旧路径只在有人 `InDanger`（当回合必死）时启动，且只认识 5 个药水名；对抗的两场团灭里，Bot 死时手里分别还捏着 **6 瓶**和 **3 瓶**，而整局只喝过 1 瓶。现在按**价值是否已经吃满**放行，并按时机分档：
  - **防御类**（`BlockPotion` / `ShipInABottle` / `Fortifier` / `BloodPotion` / `Ambergris`）：**整份剂量都被用上**（格挡不留余量、治疗不过量）且**喝完后还能活**，并且**后置**——只在全队已无牌可打时判定，否则一张牌就可能盖住这波，把"吃满"变成浪费。
  - **输出类**（`FirePotion` / `PotionShapedRock` / `ExplosiveAmpoule` / `FoulPotion`）：**击杀手牌杀不掉的敌人**即投掷，并且**前置**——用 `Preemptive` 抢在出牌计划之前，因为这一刀下去后续出牌的价值会变。
  - **卡牌生成类**（`AttackPotion` / `SkillPotion` / `PowerPotion` / `ColorlessPotion`）：它们**救不了人**，留在槽里就是纯损失，因此终局或**精英/Boss 战**直接喝；开出的选择由各端共用的确定性回退 `BotBrain.SelectCards` 回答，不需要额外握手。
  - 救援路径（真正救人）仍是最高优先级；**0.34.1 的"手牌本来就能杀就不喝"守卫原样保留**，真人未结束回合不代喝的限制也保留，理由记为 `final-encounter-no-future` / `full-value-*`。
- **关于 P1「共现降为先验」的结论**：只放行**敏捷 × 已有挡格**（0.36.0 已做），**力量不做**。原因是 `Rupture` 在档案里同样是"力量型 payoff"，但它要的是掉血触发；`CardFacts` 没有任何字段能区分"乘已有攻击"与"等一个触发"，用攻击牌数量去解除警告会把**真正缺少启动件**的牌也一起放行（这条行为有回归钉着）。充能球同理：档案里没有"球"这一角色，无从度量。两者继续走共现先验，等有可靠的条件成长信号再说。
- **E1 爬塔深度感知：不只看"第几幕"，还看"前面还剩什么"**。新增 `RunDepth`，从当前地图节点沿后继做一次有界 BFS 到 Boss，统计**前方是否还有商店/营地**（读不到地图一律按"不确定 = 不启用特判"，所以既有行为不变）。三处接上：
  - **最后一个商店把钱花光**。成交门槛由"终局 0.8×"再降到 **0.35×**（第三幕且前方已无商店）——金子在最后一场之前花不掉就是废纸。**只缩放门槛、不翻转符号**，会让牌组变差的购买依旧被拒（0.36.0 的既定原则）。实测 1.00 → 0.80 → 0.35。
  - **最后一个篝火优先血量**。Boss 房的回血权重由固定 1.6 改为**第三幕 1.8 / 更早 1.5**，并且"本幕最后一个营地"按**本幕自己的 Boss 权重**保底（不再统一拉到常数 1.8——那会把一幕 Boss 营地也抬到三幕的紧迫度，对还有两幕可长的牌组正好是反的）。实测同一处 55/73 的伤：后面是商店 11.0（升级），后面是第三幕 Boss 23.2（休息）。
  - **后期更排斥废牌与诅咒**。计划外卡牌的规模压力乘以 `BloatFactor = (1 + 0.25×幕) × (前方还有商店 ? 1 : 1.35)`：第三幕 ×1.5，第三幕且无商店 ×2.025——这种稀释再也删不掉了。**计划内/补短板/有挖掘亲和的牌不受影响**（仍 0.4），所以路线牌不会因为到手晚而抓不进来。同一张废牌在 18 张牌组下：第一幕 1.8 分（可拿），第三幕 -5.7 分（跳过，`size:22.5`）。事件里授予诅咒的选项（-14）与关键词含 `curse` 的选项（-9）同样按 `BloatFactor` 加权，诅咒删除下限从 155 起同乘——后期更愿意为删诅咒掏钱。
- **F1 局外随机数预测（移植 Random Foreseer 0.13.14）**。Bot 过去只能看见「遗物上印着什么」，看不见「它会给你什么」——磨刀石、Orrery、胶囊这类**价值就是一次随机**的遗物，在 `RelicValue` 里一律返回 0，宝箱分遗物时无从比较；选遗物界面更是**直接取第一件**。现在把 Random Foreseer 的**局外预测逻辑层**移植进内核（`src/CoopBots.Kernel/Vendor/OutOfCombat`），丢弃其全部 UI 层（Harmony 补丁、Godot 节点、设置页、遥测），预测机制原样保留：**克隆当前 RNG、复刻游戏自己的生成器，绝不推进真实随机数**。
  - **接入一：遗物按"拾取效果"计价**。`HumanCoopAdvisor.RelicValue` 原有的逐遗物白名单只覆盖"价值印在牌面上"的遗物；对随机效果类遗物，新增一层预测估值：升级类（磨刀石/战纹涂料/SandCastle/FragrantMushroom）按**这张牌真实的升级差分**求和；给卡类（Orrery/ArcaneScroll/Kaleidoscope/ScrollBoxes/GlassEye/SeaGlass）按**每个候选包取最优牌**计价；给遗物类（胶囊/PhialHolster/LostCoffer）递归一层；给药水类计固定值。变形类**暂不计价**（变形是替换而非白给，缺"被替换的那张"就无法定价，错误地当成收益会让 Astrolabe 看起来像白送三张）。
  - **接入二：宝箱分给最需要的 Bot**。`TeamCoordinator.AssignRelics` 本来就是最大化 `Σ RelicValue` 的选座搜索，估值修好后自动生效：实测同一块磨刀石在 0 张攻击牌的 Bot 值 **0.0**、1 张值 **3.6**、9 张值 **7.2**，于是分给攻击牌最多的那个。
  - **接入三：选遗物不再取第一件**。`CardChoicePatches` 里 `relics[0]` 改为按同一估值排序（Boss 遗物同样受益：Brimstone 的团队负面、胶囊的随机内容现在都参与比较）。
  - **边界**：读不到预测时一律回退到原有估值，不编造；`THIRD_PARTY_NOTICES.md` 已记录移植范围与版本许可说明。**事件选项的打分（P0-2）留作下一阶段**，本轮只交付遗物/宝箱链路。
- **回归**：普通套件 82 项、内核套件 158 项通过。新增：磨刀石预测出 2 张待升级攻击牌、Orrery 预测出 5 个候选包且估值 202、同一块磨刀石按攻击牌数量给出 0.0/3.6/7.2 并分给攻击牌最多的 Bot、选遗物界面在一件弱遗物在前时仍选出更强的那件，以及**预测前后 RNG 计数与牌组指纹完全不变**。新增：59/66 药水覆盖 + 逐瓶"用了必须改变局面"、营火在一、二幕 25% 血线上下分别锻造/休息、三幕 55% 反转、无帐篷不奶队友（除濒死）、有帐篷可连做、最后一幕买保本货而第一幕不买、23 张牌下 `needs-block` 的牌必须为正分（实测 +23.0，`block,needs-block,size:12.0`）、敏捷成长随挡格数量提升、Retain 攻击不再判成挡格成型、睡眠类怪招可被模拟、"触发计数的那一手会被扣掉它造出的状态牌"、持有伤害可格挡而生命流失不可格挡（6.0 敌方攻击 / 9.0 带 Wither / 6.0 带 Beckon）、升级过的 Wither 更贵、营火随下一间房加权、以及药水"吃满才喝"（12 点格挡吃满 14 点伤害且喝后能活才喝、9 点伤害则保留、输出药能杀而手牌本就能杀则保留、卡牌生成药只在终局或精英/Boss 战喝）、商店门槛随深度下调并在最后一个商店塌到 0.35、后期废牌更贵而计划内牌不受影响。

## 0.35.2

**复盘驱动的两项修复（A1/A2）**。安装包：`outputs/CoopBots-v0.35.2.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 preview.50 起的全部改动。）

- **A1 回合末在手伤害按"镜像实际收费的变量"计价**。永世沙漏（Aeonglass）的 `WITHER` 在本地化里写的是 `{Damage:diff()}`、镜像走 `HandleDamage`（读 `DynamicVars.Damage`），而我们两处 `HeldPenalty` 都只找 `HpLossVar`（那是 Beckon 的形状）——于是 Wither 在个体评分与快速叶节点评估里**完全隐形**，Bot 在"自己回合结束阶段"握着它掉血致死（日志 37504–37522 行的死亡早于敌方行动，与之一致）。现在 `HeldPenalty` 先读 `HpLoss`、没有再读 `Damage`，并且**两处实现合并为一份**（此前是两份拷贝，这正是它漏掉一类变量的原因）。
- **A2 删牌意愿随"死抽数量"增长**。原来的基础牌删除基线是固定 55，换算成金币 55×2.2=121，而商店第二次删牌要 150——**无论有多少金币都买不起**，于是每局都以 9–10 张初始牌收场（实测：三人合计 29/71 张基础牌留到最后一战）。现在基线乘以随初始牌数量增长的系数（10 张时 2.5×，即 137.5 → 可支付 302 金），并且仍然保留"关键唯一定位牌不可删"的硬保护。
- **回归**：新增"握在手里的 `Damage` 型牌同样被计价（`Wither` → 3 点）"、"同为 23 张牌，10 张初始牌时删牌价值必须能支付 150 金"；普通套件 74 项、内核套件 136 项通过。

## 0.35.1

**修复：路线牌被"牌组规模压力"误伤**。实机第三层三副牌都是"9–10 张初始牌 + 一条偏薄的路线"（毒线强度只有 0.43），日志里出现了**在毒牌组里跳过 `DEADLY_POISON`** 这种自相矛盾的决策。原因：判断"这张牌是在计划上的"时只看 `Archetypes.Bonus`（已识别路线），**漏掉了同样来自数据挖掘的卡牌亲和度**——于是当路线是从另一个集群识别出来的时候，明明是同一套计划里的牌也按全额规模压力计费，被压成负分而跳过。安装包：`outputs/CoopBots-v0.35.1.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 preview.50 起的全部改动。）

- **判据补齐**：`onPlan = 已识别路线 > 0 || 挖掘出的卡牌亲和度 > 0`。两条都是"这张牌和现有牌组是一套"的数据证据，此前只用了前者。
- **效果**：21 张牌时原始规模压力为 30，计划内的牌只付 40% 即 12；此前毒牌在毒牌组里也要付 30，直接被压负。
- **回归**：新增"同为 21 张牌，有挖掘伙伴的牌付 `size:12.0`，没有伙伴的付 `size:30.0`"；普通套件 73 项、内核套件 133 项通过。

## 0.35.0

**全真人阵亡后：一次超长思考，之后不再思考，按剧本打完**。此前"交给内核"只做到"每回合深搜一次、然后逐张重规划"。现在该状态下没有任何动态输入（没有真人手牌/能量会变，也没人在等），因此改为**一次性把整条世界线的最优解算完，然后按计划跨回合执行完，后续回合不再搜索**。安装包：`outputs/CoopBots-v0.35.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 preview.50 起的全部改动。）

- **超长思考**：独占状态下搜索预算单独一档——depth 40 / width 16 / nodes 131072 / CPU 28s / **墙钟上限 30s** / 投影 16 回合（人类可见的那档仍是 5 秒上限，两者互不影响）。没有人在等，所以允许它跑到自然收敛。
- **按剧本执行**：内核返回的动作序列（**含"结束回合"步骤**）会被完整保留，运行时逐步提交、**中途不再搜索**；每个步骤提交前仍用实时牌面校验（牌还在手、目标合法、持有者存活），任何不匹配立即丢弃整段脚本并回到正常搜索——脚本是预测，不是免检通行证。
- **安全边界**：`结束回合`步骤只在该回合归属团队时才会执行（真人尚在决策时绝不代其结束）；带选择的牌沿用 `BotChoicePlanSync` 的既定通道。
- **提示文本**：独占状态下，面板显示 **「机器人思考中……」**；进入执行阶段后显示 **「机器人按计划执行中……」**（英文 `Bots are thinking…` / `Bots are playing out their plan…`），避免长时间无输出看起来像卡死。
- **顺带修掉过时文案**：状态行原有"快速收尾：全队每 0.2 秒一次行动"与节奏重做后的实际行为不符，现按状态显示五种之一并中英双语。
- **不再重复空搜结论**：真人"结束回合"不改变 Bot 可见的任何东西（手牌/能量/敌方意图/抽牌堆都一样），因此内核用**只由参战者构成的指纹**（`CaptureBotStamp`）记住上一份"无牌可打"的结论；指纹未变时不再重搜，直接进入常规回退与结束回合。此前这种局面会在真人点结束的瞬间再花一次深度预算，得出同一结论。
- **回归**：新增"真人结束回合不改变 Bot 可见指纹（而完整指纹仍会变化）"、"只有真人全部不存活时才判定为独占接管"、"计划脚本可跨回合重放、用尽即止、牌离手即丢弃"；普通套件 72 项、内核套件 132 项通过。

## 0.34.1

**修复：牌能斩杀却仍丢伤害药水**。实机场景——敌人回合结束被毒到只剩 9 血，Bot 结束回合，**下回合开局先丢了一瓶爆炸药水**。原因：进攻药水（爆炸药水 / 火焰药水）的选择只看"这瓶药能杀掉哪些敌人、从而救下谁"，**从不比较"队伍手里的牌本来就能杀掉这些敌人"**，于是同一件事用药水付了钱。安装包：`outputs/CoopBots-v0.34.1.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 preview.50 起的全部改动。）

- **判据用同一把尺子**：改用评估器已经信任的 `AttackPotential`（"Bot 单靠手牌能对每个敌人打出多少"）——若药水要杀的敌人**全部**都已落在队伍手牌的斩杀范围内，就留着药水，让计划去吃这些击杀。不会比计划自己声称的更严格。
- **仍然会用药**：药水要杀的敌人手牌够不着时（例如没有攻击牌、能量不足），照旧投掷——避免为了省药水错过救援。
- **回归**：新增差分测试"手牌能斩杀 → 保留药水；手牌杀不掉 → 仍然投掷"；普通套件 72 项、内核套件 131 项通过。

## 0.34.0

**大厅与局内面板重做**：添加 Bot / 选择难度原来是手写偏移的临时控件，现在统一到一套视觉。安装包：`outputs/CoopBots-v1.0.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.33.4 的全部改动。）

- **共享主题 `BotUiTheme`**：取封面的配色（深板岩底 + 单一暖金强调色）、统一的字号层级与卡片样式（圆角 + 描边 + 阴影），并让面板继承游戏自身主题，所以文字与按钮的字体跟随游戏。
- **大厅面板**：标题带强调色竖条；**难度下方实时显示该档位改了什么**（间隔 / 思考预算 / 作弊效果），不再是三个看不懂的名字；**队伍名单**逐行列出 `Bot N · 档位 · 角色`，作弊档用强调色标出；显示**席位 X/4**；底部标注 **Mod 版本 + "所有真人需为同一版本"**；错误信息用红色，非房主显示原因而不是静默禁用；**中英双语**（跟随游戏语言）。
- **局内面板**：同样的卡片样式与内边距；状态行与标题并排；**暂停按钮现在反映状态**（暂停 ↔ 继续），不再是永远写着"暂停"；加宽的高度改为按内容自适应（不再固定 200px 截断）。
- **顺带补的 API**：`BotDifficulty.English()/Describe()/DescribeEnglish()`、`LobbyBotService.Bots(lobby)`（返回每个 Bot 的 id 与角色名，反射仍留在 service 内）。
- **回归**：普通套件 72 项、内核套件 130 项通过。

## 0.33.4

**与玩家配合的时机规则：增益要趁早、保护要等确定、目标优先队友**。安装包：`outputs/CoopBots-v0.33.4.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.33.3 的全部改动。）

- **本回合增益（力量/敏捷等一次性增益）必须尽早给**：若目标已经结束回合，这层增益随回合消失 → 直接重罚（`buff-after-end`）；目标是真人且还没结束时，仍按 0.7 折扣（我们无法强制他们使用），**队友 Bot 是可靠目标**（`prefer-team-buff`）。给队友转移能量同理（`energy-after-end`）。
- **本回合增益的识别**：优先读卡牌声明的 `Temporary*` Power；但多人牌并不声明它（`Coordinate` 声明的是普通 `PowerVar<StrengthPower>`，临时 Power 在打出时才施加），因此对这类"整体价值就在于时机"的牌维护一份精短清单（`COORDINATE`/`FADE`），并在注释里说明这是配合时机规则、不是效果建模。
- **格挡类保护等确定再打**：格挡药水只按**真正能吸收的部分**计价（`min(格挡, 未覆盖伤害)`，12 点格挡挡 5 点伤害不再按 12 计），且**真人尚未结束回合时，只在其"否则会死"时才用**——因为他们可能自己会挡住，早用就是浪费；真人结束回合后，能救下即可。
- **测试**：新增"一次性增益必须指向还能行动的队友、绝不指向已结束的玩家"；普通套件 72 项、内核套件 130 项通过。

## 0.33.3

**修正上一版对 0 能量的误判，并让"一次长考、然后快速打完"真正生效**。安装包：`outputs/CoopBots-v0.33.3.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.33.2 的全部改动。）

- **撤销 0.33.2 的"0 能量不深搜"门控**（那是错误方向：0 费牌同样可以打出长线操作，能量药水也算资源）。真正的病因是上一版把深度档的 `MaxRounds` 一并放大到 6：`EndTurn` 展开每次要模拟 6 个回合，搜索树永远无法自然耗尽，于是**任何**深搜都会顶满 5 秒墙钟，"0 费也长考"只是最明显的表现。现改为 **3**：保留真正的跨回合投影（实时档是 2），同时让大多数搜索在远早于 5 秒时自行终止——这正是"5 秒上限"应有的含义。
- **方案 A：复用长考的剩余序列**。内核把计划尾部的动作存下来（仅限**完全建模**、无边界、非 estimated 的计划），运行时在出牌动画结束后**直接提交下一张，不再重新搜索**；每一步都用**实时牌面**重新校验（牌还在手、目标仍合法、持有者存活），任何不匹配立即丢弃整段尾部并回到正常搜索。这样"一次长考"不再被每张牌的重规划稀释。
- **回归**：新增"两刀斩杀的计划尾部可复用、用尽后不再产出、牌离手即丢弃"；普通套件 72 项、内核套件 129 项通过。

## 0.33.2

**0 能量不再空跑 5 秒深度搜索**：人类结束后，只要队伍还有可花的资源才启用深度预算（有能量且能打出正费牌，或可用战斗药水）。此前 0 能量、手里只剩 0 费牌时，深度搜索照样跑满墙钟，最后结论却是"不打牌、结束回合"。安装包：`outputs/CoopBots-v0.33.2.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.33.1 的全部改动。）

- `KernelCombatPlanner.HasResourceToSpend`：有能量且存在可支付的正费牌，或存在可用战斗药水 → 才给深度预算；否则退回实时预算（0 费牌仍然会打，只是不值得为它长考 5 秒）。
- **回归**：新增"0 能量 + 只有 0 费牌 → 不深搜；有能量且付得起 / 有药水 → 深搜"；普通套件 72 项、内核套件 129 项通过。

## 0.33.1

**路线策略：成型判定、投入深度、双修门控与启动件校验**。安装包：`outputs/CoopBots-v0.33.1.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.33.0 的全部改动。）

- **成型三档**（按 mined 集群的签名卡命中数）：1 张 = 倾向（不认路线、无加成，可自由转向）；2 张 = 上路（给加成，并开始惩罚离题牌）；3 张 = 成型。`MatchClusters` 原本 `hits<2` 直接不认，现在把命中数带进 `Match`。
- **不能回头的点**：命中 2 张起，离题牌惩罚从 −4 加深为 `−(4 + 2×(hits−2))`，即拿到第二张签名卡后再抓无关牌要付实质代价。
- **双修只在数据证明会合流时**：两条路线**共享签名卡**（mined 数据里就有，如 SILENT `BLADE_DANCE` 同属两条刀线、`RICOCHET` 同属两条攻击计数线、DEFECT `CLAW` 同属两条爪线）或**共享角色**才计入第二条；否则那条 0.5 权重的对冲加分不予发放。涂毒+小刀正属此类：`ENVENOM` 与毒系强关联，而它靠"多段攻击"触发，小刀流正好供得上。
- **启动件校验（数据驱动，非卡名规则）**：若一张卡的 mined 伙伴存在、但一个都不在牌组里，它的 scaling 只按 40% 计入并标 `scaling-unsupported`。本局 Bot474 的 `RUPTURE` 正是此例——它的伙伴（Hemokinesis/Inferno/Breakthrough/Spite）全是自伤牌，一个都没有。
- **回归**：新增"Rupture 无启动件被标记且估值更低、补上 Hemokinesis 后回升"；普通套件 72 项、内核套件 128 项通过。

## 0.33.0

**构筑目标函数统一：跳过拿牌 / 不买某张牌 / 删牌，都按"离目标构筑更近"判断**。此前"跳过"只在极端臃肿牌组（25 张全饱和）才触发，真实牌组（20 张、角色混合）永远不跳过——上一局 36 次拿牌 0 次跳过。安装包：`outputs/CoopBots-v0.33.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.32.3 的全部改动。）

- **规模压力**（`BuildValue.Add`）：牌组超过 15 张后，每多一张的"再来一张好牌"成本线性上升；**路线牌只承担 40%**（`OnPlanRelief`），所以"计划要的牌"仍进得来，而"什么都不推进的填充牌"会跌破 0 → 真正跳过。理由行带 `size:N`。
- **阈值用真实日志标定**，不是拍数字：19 张牌时最优候选仅 18–20 分（应当跳过）、25 张时仅 14 分；而 20 张牌出现 46 分强牌、21 张出现 28 分路线牌（必须留下）。据此定 `FormingSize=15 / SizePressure=5.0`。
- **三处决策自动统一**：奖励跳过走 `BestReward(allowSkip)`；商店买牌用同一个 `Add`（大牌组下中档卡 `Add≤0`，商店根本不再考虑购买）；删牌用同一个 `Remove`，因此金币自然从"再买一张"转向"删一张"。
- **卡组快照新增路线名**：`route=Name(0.75)+Name2(0.30)`（两条=在对冲，`none`=尚未收敛）；候选理由变成 `fits-route:StrengthIronclad`，可直接审计"是否在向某个成型构筑靠近"。
- **回归**：新增"同一张牌在 20 张牌组必须低于 9 张牌组、且填充牌跌破 0"；普通套件 71 项、内核套件 127 项通过。

## 0.32.3

**构筑日志补上"路线名"**：`fits-route` 只写"符合路线"、不写是哪条，无法审计是否在向成型构筑靠近。现在候选理由带路线名（`fits-route:StrengthIronclad`），每幕/终局牌组快照带 `route=Name(强度)`（两条说明在对冲，`none` 说明尚未收敛）。

## 0.32.2

**加入卡牌构筑埋点**：此前日志里关于构筑只有 `deck=N` 一个数量，没有任何"拿了什么、为什么、跳过没有"的记录，导致"Bot 构筑没变好"无法复盘。现在补齐（前缀统一为 `CoopBots build`）。安装包：`outputs/CoopBots-v0.32.2.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.32.1 的全部改动。）

- **终局牌组**：`CoopBots build[final]` —— 每位 Bot 的完整牌组（按卡名排序、同名合并为 `xN`、升级以 `+` 标注）。
- **每幕快照**：`CoopBots build[act1|act2|act3]` —— 进入每一幕时的牌组，便于对比"预期构筑 vs 实际抓到的"。
- **每次拿牌/跳过**：`CoopBots build: reward` —— 候选牌逐张给出**估值与理由**（`>BASH=42.3(damage,team-fit)`），并标明 `pick=` 或 `SKIP`。
- **每次永久改牌**：`CoopBots build: edit` —— 删牌/升级/变换/锻造等，给出 `purpose`、选中项、以及候选的分数与理由。
- **实现**：新增 `BuildTrace`（全部埋点包在 try/catch 里，**埋点绝不会影响对局**）；接入 `BotBrain.ChooseCardReward`、`BotBrain.Permanent`（`FromDeck*` 的删/升/变换路径）、`BotRuntime` 的每幕与终局快照。
- **回归**：普通套件 70 项、内核套件 126 项通过。

## 0.32.1

**加深"人类结束后"的那一次深度搜索**：玩家全部结束后，队伍每回合获得**一次**更深的前瞻搜索，最长思考时间 **5 秒**上限，然后按上一次的快速重规划把剩余的牌打完（不再每张牌各跑一次深搜）。安装包：`outputs/CoopBots-v0.32.1.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.32.0 的全部改动。）

| 参数 | 旧（深度档） | 新 |
|---|---|---|
| 深度 depth | 16 | **24** |
| 节点 nodes | 3072 | **12288** |
| CPU 预算 | 1200ms | **4500ms** |
| 墙钟上限 | 600ms | **5000ms（硬上限）** |
| 回合数 rounds | 4 | **6** |

- **硬上限**：Pro 档的思考系数 ×1.5 也不会超过 5 秒；Flash ×0.5 → 2.5 秒，按档位自然缩短。
- **顺带修掉一个遗漏**：`KernelTeamSearch.Options.MaxRounds` 默认是 2，而调度层从来没把它传进去——深度档号称投影 4 回合，实际搜索只跨了 2 个回合边界。现在按档位传入（深度 6 / 实时 2）。
- **代价说明**：这 5 秒只在**人类全部结束后的第一次**规划里花；同回合后续计划仍走实时预算，所以"想一次、快速打完"的结构不变。若残局（精英/BOSS）实测发现 5 秒仍不够，可再调 `MaxDeepWallMs`。
- **回归**：普通套件 70 项、内核套件 126 项通过。

## 0.32.0

**打牌节奏重做：想完就打、结束回合一起结束**。此前 `CombatPacing` 的门是"每次出牌前都要等满间隔"，即使 AI 早就想好了；玩家结束回合后每张牌还要再等 0.3 秒，而且**三个 Bot 的结束回合是每隔 1.5 秒逐个提交的**。现在：

- **间隔只用于"没事可做时的重查节流"，不再挡在出牌前面**。只要队伍手里有可出的牌、或内核搜索正在进行，一想完就立刻出牌；只有 idle（无牌可出且上一拍也没动作）时才按间隔重查，避免逐帧空转。
- **人类全部结束后不再有任何等待**（`Effective` 快速档 = 0），队伍以思考速度把剩下的牌打完。
- **结束回合一次性提交**：所有可结束的 Bot 在同一拍一起 `EndPlayerTurnAction`，不再逐个隔 1.5 秒。
- **深度搜索每个回合只做一次**：人类结束后第一份计划用深度预算（投影 4 回合），之后同回合的计划退回实时预算——此前**每张牌都重跑一次 600ms 上限的深度搜索**，这正是"玩家结束后要等很久"的主因。
- **顺带修掉一个真 Bug**：节奏门会在搜索进行中把 `_planStartedAt` 清 0，导致计划耗时记账丢失。

安装包：`outputs/CoopBots-v0.32.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.31.6 的全部改动。）

- **回归**：`PacingScenarios` 重写为"人类决策中按 1.5s/0.5s、结束后零等待、暂停优先、换轮/重连重置、计划耗时抵扣等待"；普通套件 70 项、内核套件 126 项通过。

## 0.31.6

**补齐 FLANKING / KNOCKDOWN（队友伤害倍率）**：这两张牌此前内核完全无法模拟（Flanking 是显式边界、Knockdown 的镜像直接抛 NRE）。现在把"队友攻击增伤"按**易伤的同一条乘法通道**建模：目标身上有该 Power、且攻击者是**其他玩家**时才生效，施法者自己的攻击不受益。与易伤不同的是它们**单回合内指数叠加**——实测 3 层夹击 = **8 倍**（基础 10 → 80），1 层 = 2 倍；Knockdown 单次 2 倍、升级 3 倍。内核未建模卡 **7 → 6**，异常镜像 **2 → 1**。安装包：`outputs/CoopBots-v0.31.6.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.31.5 的全部改动。）

- **新增 `TeamDamageMirrors`**（非 Vendor）：在 `ModifyDamageMultiplicative` 上注册 `FlankingPower`（`2^层数`）与 `KnockdownPower`（Power 自身记录的倍率），条件与易伤一致，另加"攻击者须为玩家且非施加者"。
- **Flanking 的施加者记录**：`SimulatedCombatState.ApplyPower` 原本只为 Knockdown 记录 `Applier`，现同步为 Flanking 记录，否则无法把施法者自己排除在加成之外。
- **顺带修掉 Knockdown 的 NRE**：`CardEffectSpecRegistry` 里一处未适配的 `PlatformUtil.GetPlayerName` 会用实时平台名覆盖模拟器已算好的 `Applier`，在非 Steam 主机下抛空引用。现在由模拟器自己的玩家名表决定。
- **差分测试**：1 层 2×、3 层 8×、施法者自身不吃加成、Knockdown 2×/3×（升级）。这道测试是这两张牌离开"必须保持边界"安全名单的前提。
- **回归**：普通套件 70 项、内核套件 126 项通过；导入脚本 anchor 校验通过。

## 0.31.5

**补齐多人牌建模（A 类 9 张全done）**：`Cacophony`、`Underworld`、`Soulbound`、`Hibernate`、`BladeSymphony`、`GlimpseBeyond`、`LegionOfBone`、`Plot`、`DemonicShield` 全部进入内核。这些牌的底层 Power hook（抽牌计数、冰霜共享、生成触发、受伤触发）本已存在于快照，缺的只是 OnPlay 那一层。内核未建模卡从 **16 张降到 7 张**（`modeled 540→549`）。安装包：`outputs/CoopBots-v0.31.5.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.31.4 的全部改动。）

- **逐卡效果**（均按字面语义实现，并逐条断言，不只看"能否打出"）：Cacophony 抽牌计数 Power；Underworld 灾厄转化 Power；Soulbound 落在选定队友身上并记录施法者；Hibernate 冰霜共享 Power（冰霜球走既有 `OrbChannel`）；BladeSymphony 给**所有玩家**各发小刀到手牌；GlimpseBeyond 给**所有玩家**抽牌堆各加灵魂；LegionOfBone 让**所有玩家**各召唤 Osty；Plot 给**所有玩家**各挂"下回合抽牌"；DemonicShield 施法者付血、选定队友获得等同其格挡的护甲。
- **安全网**：`KernelEngineScenarios.AuditMultiplayer` 有一道"未被差分验证的多人牌必须保持边界"的断言；DemonicShield 离开该名单时，先补上了它的差分测试。
- **导入脚本**：`add_team_card_support` 以 4 组上游 anchor 重新注入以上全部改动，anchor 数量校验失败即中止导入。
- **回归**：普通套件 70 项、内核套件 125 项通过。

## 0.31.4

**补齐多人牌建模（第 1 张：OUTRAGE）**：`OUTRAGE`（造成伤害，并给所有玩家弃牌堆各加一张复制）此前内核完全无法模拟，是实机日志里命中次数最高的未建模牌（单场 712 次边界），导致内核在二三层精英战持续回退、且无法评估它污染全队牌组的代价。现在内核能模拟"伤害 + 每名玩家各拿一张复制"。安装包：`outputs/CoopBots-v0.31.4.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.31.3 的全部改动。）

- **扩展点**：`Vendor/Prediction/CardEffectSpecRegistry.cs`（多人卡正式扩展点，由 `scripts/import-combatsolver-kernel.py` 的 `add_outrage_support` 在重导时重新注入）。加入 `GenerationEffects` 后 `PredictionCoverage` 才会把它标为已补偿，否则边界依旧存在。
- **验证**：新增"OUTRAGE 必须造成伤害且每名玩家弃牌堆各有一张复制"的回归；覆盖率探测从 `unmodeled=17` 降到 `16`、`modeled=539→540`。

## 0.31.3

**修复药水浪费**：内核搜索只评估"用药后的局面"、不评估"消耗掉一瓶药"，于是把药水当免费资源。实测（日志 `planned potion first: VULNERABLE_POTION (6 actions)`）：BOSS 剩 15% 血、身上已有 4 层易伤，Bot 仍喝易伤药水，下回合阵亡且药水槽已空。现在**目标已有多层易伤/虚弱时不再重复施加**（除非该计划本回合能斩杀）。安装包：`outputs/CoopBots-v0.31.3.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.31.2 的全部改动。）

- **根因**：易伤药水不在 `BotPotionPlanner` 的防御/斩杀/虚弱分支里，只能由内核"主动用药"路径打出；该路径此前**没有冗余检查**。重复施加只延长持续时间，局面几乎没有变化，但搜索仍愿意消耗药水。
- **修复**：`RedundantDebuff`——目标存活且已有 >1 层对应易伤/虚弱时判为冗余，除非该计划本回合将其斩杀（此时额外易伤确实在做事）。保留 1 层不拦，避免挡掉正当的续层。
- **未做**：药水的整局机会成本（用掉一瓶 vs 留给下场精英/Boss）是计划 W5 / P2#11 的内容，本次不做粗略门槛，以免误伤 `ENERGY_POTION`/`SWIFT_POTION` 这类正当的节奏用药。
- **诊断**：战斗汇总新增 `potion-declined`，用于在实机日志里核对这条规则的命中情况。
- **回归**：普通套件 70 项、内核套件 123 项通过；构建期类型加载守卫通过。

## 0.31.2

**修复 MOD 无法加载（ReflectionTypeLoadException）**：`HumanFinisher` 的迭代器把 `(CardModel, KernelSession, int)` 元组提升进状态机；`ValueTuple` 是值类型，运行时要算它的 GC 布局就必须解析 `KernelSession`，而游戏在调用初始化器**之前**枚举所有类型、`CoopBots.Kernel` 要到初始化器里才加载——于是整个 MOD 加载失败。改为引用类型的 record 后不再有加载期依赖。安装包：`outputs/CoopBots-v0.31.2.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.31.1 的全部改动。）

- **根因**：`ModManager.TryLoadMod` 在调用 `[ModInitializer]` 之前执行 `Assembly.GetTypes()`；我们的 kernel 是**延迟加载**的（`KernelAssemblyLoader`），所以任何在类型加载期就需要 kernel 的类型都会让 GetTypes 抛 `ReflectionTypeLoadException`。
- **触发点**：迭代器/async 状态机里的局部变量会被提升为状态机字段。字段类型若是**值类型且包含 kernel 类型**（如 `ValueTuple<CardModel, KernelSession, int>`），CLR 计算结构布局时就必须解析 kernel 程序集。引用类型字段没有这个布局要求，因此 `HumanFinisher` 内改用一个私有 `record FirstStep(...)` 即可。
- **新增构建期守卫** `tests/TypeLoadCheck`：在**独立进程**（其 app 目录不含 kernel）中枚举 `CoopBots.dll` 的所有类型，kernel 保持不可解析；一旦有类型在加载期依赖 kernel 就直接让构建失败。已用坏版本验证会 exit 1、修复版 exit 0。
- **回归**：普通套件 70 项、内核套件 122 项通过；构建期守卫通过。

## 0.31.1

**内核不再被无关通知打断**（优化计划 W1 的第一项）：战斗通知计数此前被当成"局面已变"，导致内核正在展开的深度搜索被整段丢弃。现在只有**排队的新动作**或回合/焦点/参战者变化才会中止搜索，真正的状态变化交给完成时的状态指纹校验兜底。安装包：`outputs/CoopBots-v0.31.1.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.31.0 的全部改动。）

- **改动**：`KernelCombatPlanner.Current()` 不再比较 `KernelSession.LiveRevision`。该计数在每次 `CombatStateTracker.NotifyCombatStateChanged` 时自增，其中大量通知并不改变分支依赖的状态；以它判定过期，是深度搜索被浪费的最大单一来源。
- **安全兜底不变**：搜索完成、部署计划之前，仍用 `KernelSession.CaptureLiveStamp`（回合/生命/格挡/能量/牌堆/权能/遗物/RNG 的完整指纹）与捕获时对比。真实局面变化仍会丢弃计划并回退，不会打出基于旧局面的牌。
- **代价**：真实局面变化时，可能多花掉本次搜索的剩余预算再回退。这是为"不再误丢有效搜索"付出的成本，且受既有预算上限约束。
- **新增基线指标**：每场战斗汇总新增 `coverage`（可用计划占已结算搜索的比例）与 `boundaries`（本场截断搜索最多的三类卡牌/机制），作为后续优化与模型补全排序的对照基线。
- **回归**：新增"纯通知churn 不得改变内核结果（应与无 churn 完全一致）"；排队动作导致的回退仍照旧。普通套件 70 项、内核套件 122 项通过。

## 0.31.0

**难度分级重做**：旧的 Dumb / Normal / Smart / Genius 由 **Flash / Pro / Cheated** 取代。所有档位跑**同一套完整算法**，差别只在节奏与思考预算；Cheated 额外获得 3 倍金币。安装包：`outputs/CoopBots-v0.31.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.30.2 的全部改动。）

- **旧的"故意变蠢"行为全部删除**：随机选奖励/事件/营地、看运气提前结束回合等分支不再存在。低档位不再等于"打得差"，而是"想得少、出得快"。
- **Flash**：每张牌间隔 **0.5s**，内核思考预算 ×0.5；**Pro**：间隔 **1.5s**，思考预算 ×1.5；**Cheated**：沿用 Pro 的节奏，额外 **3 倍金币**。
- **思考预算是纯时间/节点预算**（`KernelCombatPlanner` 的节点数、CPU、墙钟），**搜索形状（深度/宽度/回合数）不变**，所以快档只是提前收手，不会规划出结构上更差的路线。人类全部结束回合后仍按原比例提速（×0.2）。
- **团队按最强档位统一调度**：一队里只要有一个 Pro/Cheated，团队就按 Pro 的节奏与预算跑，避免 Pro 被 Flash 拖慢。
- **作弊（3× 金币）**：补丁 `PlayerCmd.GainGold`，凡是 Cheated Bot 获得的金币 ×3；被偷回的金币（`wasStolenBack`）不重复翻倍。判定完全由 NetId 派生，所有端一致，不需要新增存档字段。
- **旧存档兼容**：难度仍编码在合成 NetId 的 bit 16-17；新的作弊标记放在独立的 bit 18。旧档里的 Smart=2 / Genius=3 只会被读成 Pro，**绝不会**被误读成 Cheated。
- **回归**：难度往返（Pro/Cheated）、旧档 bit 2/3 夹到 Pro、两条节奏档的计时（Pro 1.5s / Flash 0.5s、结束回合后 ×0.2、计划时间抵扣等待）。普通套件 70 项、内核套件 124 项全部通过。

## 0.30.2

**团灭根因定位并修复**：一回合结束握在手里的惩罚牌（Beckon）此前**从不被打出**，等于每回合白挨伤害。安装包：`outputs/CoopBots-v0.30.2.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.30.1 的全部改动。）

- **实机日志复盘（0.30.1，22 层团灭）**：一层 BOSS `SOUL_FYSH`（211 血，打了 9 回合）里 **两个 Bot 阵亡**、第三个剩 2 血；进入二层时全队已残（74/70/61/61），随后 THIEVING_HOPPER、TUNNELER 各再掉约 20 血，二层普通战 `HUNTER_KILLER` 第 4~5 回合真人 + 两个 Bot 阵亡。**九回合打不死 211 血的 BOSS 才是问题的核心。**
- **根因**：BOSS 的 `BECKON_MOVE` 每次塞 **2 张 `Beckon`**。`Beckon` 是 1 费状态牌、**没有任何 OnPlay 效果**，但 `HasTurnEndInHandEffect => true` —— **回合结束时若还在手里，对持有者造成 6 点不可格挡、不可增幅的伤害**。规避方式只有一个：**花 1 费把它打出去**。日志证据：**真人打出 Beckon 11 次，三个 Bot 一次都没打**（`grep "Player ... playing card BECKON"` 只命中真人）。每个 Bot 每回合握 1~2 张 = 每回合白掉 6~12 血，九回合约 54~108 点——与两个 Bot 从 58~74 血阵亡完全吻合。
- **两处都漏了**：
  1. 个体评分 `GeniusCombatStrategy` 有 `card.Type is Curse or Status → Score = -500`，把"状态牌"一律当成"打了有害"，于是**永远不打**。
  2. 内核虽然能模拟 `OnTurnEndInHand`（`CombatPredictionSimulator.EndTurn` 会调用它），但**只在回合结束的节点才结算**；实战里 Bot 是在真人回合内行动（`IncludeEndTurns=false`），内核从不结束回合，因此看不到"握着要掉血"，打 Beckon 只算浪费 1 费 → **也不会打**。
- **修复**：
  - 个体评分改判：状态牌若 `HasTurnEndInHandEffect` 且有 `HpLoss` 变量，按"打出可避免的伤害"给正分（`HpLoss × 6 − 费用 × 2`），理由 `avoid-hold-penalty`；普通状态牌仍是"不可打出"。**规则按机制而非按卡名**，任何同形状的牌自动适用。
  - 内核评估新增 `HeldPenalty`：把"当前手持的回合结束惩罚"计入战损，使"打出去"能立刻降低评分代价；**结算后的节点不再计**（避免与模拟里真实结算重复扣一次）。
- **回归**：`Beckon` 必须被识别为持有惩罚；打掉它必须让内核评分上升；个体评分必须选择打出 Beckon（实测 6 点惩罚）。

## 0.30.1

按外部审查（`outputs/bot-build-review-preview60.md`）修掉 **C1 正确性 7 项**。安装包：`outputs/CoopBots-v0.30.1.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.30.0 的全部改动。）

- **升级入口不再分裂**（审查 #1，最严重）：此前"该不该锻造"用新的 `UpgradeDelta`，"锻造哪张"却走旧的 `ScoreDeckCard`——两套算法，审查复现出 Bash(5.4) 输给 TrueGrit(2)。现在 `BotBrain.SelectCards` 对**永久牌组变更**（`FromDeck*` 的强化/删牌/变牌）显式走构筑估值：强化取 `UpgradeDelta` 最高、删牌取 `Remove` 最高、变牌取边际最低。**战斗内选牌（`FromHand*` 弃牌/消耗）保持原评分**，且 `BotChoicePlanSync` 仍有优先权。
- **团队加分接回奖励/商店**（审查 #5，我引入的回归）：`TeamCoordinator` 的援护牌（`AnyAlly` +26/+10 按重复递减）与全队铺垫牌（Shockwave/Uppercut/LegSweep/Bash +15）抽成共享的 `TeamBonus`，`BuildValue.Marginal` 也计入。此后奖励、商店、团队三条路径对同一张牌不会再有分歧。
- **删牌与加牌统一到金币单位**（审查 #3a，我引入的）：加牌走 `Add()×2.2`，删牌却拿原始分直接比金币价 → 普通 Strike 保底 55 永远打不过首删 75。现在两者共用 `GoldFor()`。
- **`Duplicates` 不再把牌自己算进去**（审查 #4 顺带点出）：原来已在牌组里的牌每张都被扣 5，而候选牌不扣——**系统性偏向"加牌"**，正是 Skip 罕见的成因之一。
- **升级不再给"移除 Exhaust"扣分**（审查 #2）：旧代码 `-LostRoles×6` 让 Hologram 变 −4；现在失去 Exhaust 记 **+7（可循环）**，并新增"被移除的关键词"差分，Exhaust 只经角色计一次避免重复。
- **关键组件保护改为硬拒绝**（审查 #3b）：删除"唯一防御/AOE/能量源/过牌"不再只是扣分（11 Strike + 唯一 Defend 删它仍得 43），而是直接返回 0；测试也从"断言理由含 protected"改成**断言结果**。
- **版本号不再漂移**（审查限制项）：`ModEntry.Version` 改为**从随包分发的 `mod_manifest.json` 读取**，读不到才用编译期回退值；另加回归把回退值与仓库 manifest 对齐。

回归：新增/改写的断言**全部检查最终行为**——强化目标必须与估值排名一致、唯一防御删牌得 0 且不值金币、移除 Exhaust 的升级为正分、删牌金币价必须超过 75、奖励路径带团队加分且单人牌组不带、版本号与 manifest 一致。

## 0.30.0

事件处理收尾：**4 个高风险事件专用 handler + 事件选项 id 漂移护栏**。安装包：`outputs/CoopBots-v0.30.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.29.2 的全部改动。）

- **新增 4 个事件 handler**（现在共 7 个专用 handler）：
  - **POTION_COURIER → 永远 RANSACK**。`GRAB_POTIONS` 给的是 **Foul Potion**，喝下去会伤害包括饮用者在内的所有非宠物单位——是个陷阱选项，而通用评分看不出（键名里没有任何提示）。
  - **MORPHIC_GROVE → 金币 >120 时选 LONER**。`GROUP` 会**花光全部金币**换 2 次随机变牌，而 `LONER` 是免费的 +5 最大生命；金币能换删牌和遗物，比两张随机牌值。金币很少（≤120）时反而选变牌。
  - **ABYSSAL_BATHS → 血量够就泡**。每次浸泡 +2 最大生命但受到递增伤害；通用评分看到"damage"就早早退出，白白放掉永久上限。规则：受伤后仍高于 35% 上限就继续，否则退出（伤害读不到时按 50% 保守判定）。
  - **COLOSSAL_FLOWER → 血够就往下挖**。奖励 35→75→135 金币（或遗物），每挖一步受 5/6/7 点不可格挡伤害。选项 id 里没有可评分的词，通用评分**永远立刻取现**，放弃递增奖励。规则：血量 ≥40% 上限就继续挖，否则取现。
- **事件选项 id 漂移护栏**：烘入 `BakedEvents.cs`（`/api/events`，56 个事件的全部选项 id，含各页），handler 必须声明自己匹配的 id，测试断言**每个 id 都存在于当前版本**。理由：handler 靠 id 字符串匹配，补丁改名会让它**静默失效**——看起来接好了却再也不触发。这条护栏覆盖全部 7 个 handler。
- 只烘 id，**不烘标题/描述**——那是本地化文本，任何决策都不得依赖解析它。

## 0.29.2

修复**占卜（CrystalSphere）**：此前 Bot 付了钱（或吃一张永久诅咒）却**一个奖励都拿不到**。安装包：`outputs/CoopBots-v0.29.2.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.29.1 的全部改动。）

- **根因**：`CrystalSphereMinigame.PlayMinigame()` 的**整个函数体**在 `if (LocalContext.IsMe(_owner))` 里（`CrystalSphereMinigame.cs:149`）。Bot 不是本地玩家 → 屏幕不打开 → 函数立即返回 → 事件直接 `SetEventFinished`。**Bot 付了 51–99 金币、或吃下一张永久 Debt 诅咒，格子里 15 个物品一个都拿不到。** 我还系统排查了其余所有带 `LocalContext.IsMe` 的事件（DollRoom / Trial / Vakuu / DenseVegetation / JungleMazeAdventure / TheArchitect），**它们都只是表现层**（音效/震动/肖像/访问计数），真实效果在 `IsMe` 之外——占卜是唯一把奖励整个藏在本地界面后面的。
- **修法（同步房主的水晶球互动）**：棋盘由本局 RNG 播种，**各玩家格子里的物品与位置相同**，所以房主的操作就是一份可用脚本。
  1. 记录房主点击（工具 + 坐标），点到最后一次占卜时标记"房主已下完"。
  2. Bot 的 `PlayMinigame` 改为：**等待房主下完（上限 60 秒）→ 在同一坐标重放同样的点击** → 拿到同样的物品。房主没有脚本时回落到**确定性扫描**（Big 工具、3 格步进、次数不超过可用占卜数），保证 Bot 永远能完成、且不劣于修复前。
  3. 奖励走**游戏自己的** `OneOffSynchronizer.OfferCrystalSphereRewards`，所以稀有度、钩子与本地玩家完全一致。
  4. **跨端同步**：游戏自带的 `CrystalSphereRewardsMessage` **不能复用**——它的处理器用*发送者*推断受益者，只能给自己发奖励。新增 `BotCrystalSphereMessage` 携带该 Bot 的物品清单，各端据此为同一名 Bot 生成同一套奖励。
- **入口选择**：新增 `CrystalSphereHandler`，**永远选花金币的 `UNCOVER_FUTURE`、绝不选带永久 Debt 诅咒的 `PAYMENT_PLAN`**。理由：棋盘是本地界面，诅咒是永久牌组成本，而多出的 3 次揭示只多换几个格子物品。测试在两种选项顺序下都断言这一点。
- **失败安全**：任何一步异常都只记日志，事件照常以"无奖励"结束——即修复前的行为，不会卡住对局。
- **回归**：入口永不吃诅咒（两种顺序）、回落扫描不越界且次数不超过可用占卜数。**需要实机 4 人局验证的部分**：重放时序（房主与 Bot 的事件是各自独立实例）与跨端奖励一致性——我无法在单机测试台架覆盖。

## 0.29.1

步骤2 落地：**卡与卡的抓取亲和（draft affinity）**，429 张卡 / 1461 对搭档。安装包：`outputs/CoopBots-v0.29.1.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.29.0 的全部改动。）

- **数据来源**：`/api/draft-recs/cards/{id}`，由 `scripts/fetch-spire-codex-draft-recs.py` 抓取（540 次请求，0 失败，51 张无数据）。烘入 `src/CoopBots/Building/BakedCardAffinity.cs`。
- **限速**：该端点自身预算为 **300/分钟**（响应头实测）。脚本按 0.55s 间隔（≈110/分钟）发送，读取 `x-ratelimit-remaining`，低于 40 时自动放慢到 1.4s；429 时读 `Retry-After` 退避重试（最多 3 次）；**每卡单独缓存，可断点续跑**。全程 `remaining` 维持在 260–296，0 失败。
- **筛选口径（可信度关键）**：只烘同时满足 **`offers≥250`、`picks≥25`、`lift≥1.35`** 的搭档，每卡最多 4 个。原始数据里有的搭档只有 6 次 picks 却给出 lift 2.14，纯噪声；门槛常量写进生成文件并由测试断言未被调低。
- **用法**：`lift`（pref/pref_base，即"共同被选中的概率相对随机"）是信号——它已被归一化，不像裸胜率那样只是奖励"出现在好局里的卡"。牌组里已有某搭档时，候选卡 +`1.2×min(lift,2.5)`，最多计 2 个搭档、上限 +6。**刻意压低**：一个强的角色契合值 8~14，所以亲和只能左右接近的取舍，不能主导决策。
- **`winrate` 不烘**：与流派表同一理由——受"谁在什么情况下才拿"混淆。
- **版本漂移实测并处理**：该 feed **跑在游戏前面**，引用了我们安装版本没有的 `SCARE`。新增 `scripts/import-spire-codex-affinity.py` 读取由交叉校验套件导出的 `local-card-ids.txt`（596 张本地卡）**在生成时剔除未知引用**；测试仍保留严格断言（461 个被引用的 id 必须全部存在于当前版本），失败信息会提示重新烘表。
- **授权**：`THIRD_PARTY_NOTICES.md` 已扩展到覆盖第二个 feed（PolyForm Noncommercial 1.0.0 + Required Notice + API 条款 + Mega Crit 归属），随发布包分发。

## 0.29.0

步骤1 落地：用 **Spire Codex 真实对局挖掘出的流派簇**替换手写路线表。安装包：`outputs/CoopBots-v0.29.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.28.1 的全部改动。）

- **烘入 62 条真实流派簇**（5 职业各 12~13 条）：`src/CoopBots/Building/BakedArchetypes.cs`，由 `scripts/import-spire-codex-archetypes.py` 从 `https://spire-codex.com/api/runs/archetypes`（快照 built_at 2026-08-22，游戏 v0.111.0）生成。每条 = 3 张**定义卡** + 2 件**定义遗物** + 名称 + 样本量。
- **识别方式改为"签名"**：牌组里出现某簇 **≥2 张定义卡**才算命中（定义遗物只做确认，不替代卡）。这比按标签共现精确得多——它指名了真正的关键卡。
- **加成**：候选卡**就是签名卡**时 +14×强度，`×排名权重`（第二方向减半）；否则若其角色与该簇已贡献的角色重合，+8×强度并按已有张数递减。仍保留"完全不搭方向"的小幅惩罚。
- **标签路线降级为回退**：当没有簇命中时（改版卡、非主流牌组、feed 未覆盖的角色如 `Deprived`）才用原来那 11 条角色路线。两者都只做加法，**都不命中时加成恒为 0**，逐字节回落。
- **刻意不用胜率**：该 feed 的 `win_rate` 在不同簇之间量纲不一致（有的 0.8、有的 53.1），无法安全加权；`share` 只用于生成时排序，避免把"流行"当成"强"。
- **漂移护栏**：新增回归断言 62 条簇引用的**每一张定义卡都存在于当前版本**——补丁删卡会让签名静默失效，所以直接让测试报错。
- **授权**：按 PolyForm Noncommercial 1.0.0 与 Spire Codex API 条款使用（社区用途、非商业、需附 notice）。`THIRD_PARTY_NOTICES.md` 已加入 Required Notice + 许可 URL，随发布包一起分发；同时注明游戏数据版权属 Mega Crit Games。未复制其任何源码，只读取公开 JSON。
- 重新烘表：`python scripts/import-spire-codex-archetypes.py`（需先把 feed 存到 `work/spire-codex/archetypes.json`，该目录不进源码包）。

## 0.28.1

用外部卡表交叉校验我们的结构化解析器，**查出并修掉两个构筑估值 bug**。安装包：`outputs/CoopBots-v0.28.1.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.28.0 的全部改动。）

- **修掉 `BuildValue` 缺失的诅咒/状态守卫**（严重）。老的 `CardValue` 里有 `-1000` 守卫，我重写时漏了：诅咒的 cost 被钳到 0，于是"基础分 + 稀有度 + 零费奖励"会让一张诅咒**看起来值得拿**。现在诅咒/状态/不可打出牌一律 `-1000`，并补了回归。
- **修掉删牌符号错误**。`Remove` 原写成 `marginal + relief`；正确是 `U(去掉它) − U(现在) = −marginal + relief`。对负面牌（诅咒、被淘汰的过渡牌）方向正好反了，导致"该删的牌看起来不值得删"。
- **修掉不可打出牌被标成 `zerocost`**：分析器把不可打出牌的费用钳到 0，于是被当成零费牌计入流派使能标签。现在按 `Unplayable` 关键词判断，不看数字。
- **新增交叉校验工具** `CodexCrossCheckScenarios`：读取外部卡表（`COOPBOTS_CODEX_CARDS` 或 `work/spire-codex/`，**缺失时 SKIP，不进发布包**），逐字段比对我们的解析结果。576/577 张卡按 id 匹配成功、0 张解析失败。

**校验结果与我们解析器准确率的结论**：费用 529 一致 + 31 张"不可打出"（对方记 −1，我们钳 0，表示差异）；类型 574/576；伤害 177/199、格挡 65/72（差异集中在 X 费与 `CalculatedVar` 类卡，属设计差异）；多段攻击 19/20；**能力读取 51/111**——即我们只覆盖了约一半带能力的卡，这是当前真正的分析盲区（Thorns/Intangible/Buffer/Accuracy 等都不读）。

**顺带确认了外部表本身的漂移与错误**：`SCARE` 我们本地没有（对方多一张卡），12 张稀有度、2 张类型、若干费用对不上——**与安装版本并不完全一致**；且 `NORMALITY`(codex damage=3)、`NO_ESCAPE`(codex damage=10) 是对方把 `CalculationBaseVar` 误当伤害，**我们读 0 才是对的**。所以它不能当真相来源，当校验工具正合适。

## 0.28.0

构筑管理重构 步骤2：**引擎/流派感知**（标签全部从卡自身推导，无外部评分表）。安装包：`outputs/CoopBots-v0.28.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.27.0 的全部改动。）

- **细化标签**：在原有 13 个角色标签之上，从卡自身数据推导更细的标签——`strength` / `dexterity` / `focus` / `multihit`（新增 `CardFacts.Hits`，此前多段攻击无法识别）/ `retain` / `zerocost` / `shiv` / `minion` / `osty`。全部来自 DynamicVars 与 keywords。
- **流派识别（`Archetypes`）**：11 条机制路线，每条 = 收益标签 + 使能标签 + 阈值。**收益件没有使能件就不算路线**（多段攻击只有在牌组真的在加力量时才算力量计划，否则只是好攻击）。强度按密度缩放，重复按递减计。
- **加成而非替换**：命中的路线给收益件 +12×强度、使能件 +8×强度（次要方向 ×0.5），并按已有张数递减；**`undefined` 时加成为 0，估值与步骤1逐字节一致**——这条有专门回归保护。同时轻微惩罚"完全不搭牌组方向"的牌。
- **没有引入任何外部数据**：路线表只有 11 行、写死在代码里，判定完全依赖我们自己的结构化标签，因此 mod 卡也能被正确归类；识别不出就退回原行为。

回归：填缺角色值得拿；饱和角色低于跳过；无候选可改善时选择跳过；广泛升级胜过单纯面板升级；升级与"再加一张"是两个量；删牌命中稀释牌并保护唯一防御；**同名牌组里"再加一张"被拒而"升级已有"值得**；多段+力量的牌组被识别为 multihit 路线且多段牌更值钱；**无路线牌组零加成**。

## 0.27.0

构筑管理重构 步骤1：**统一构筑估值 + Skip 成为正式候选 + 真实升级差值**。安装包：`outputs/CoopBots-v0.27.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.26.0 的全部修复。）

新增 `src/CoopBots/Building/`（`CardProfile` + `BuildValue`），拿牌/删牌/升级/商店投资第一次用**同一个目标函数**，全部相对于"当前牌组"求差值：

- **Skip 成为正式候选**。拿牌价值 = 这张牌自身的贡献 **减去它从其他牌手里抢走的抽牌**（`max(0, 牌组均值 − 本卡) × 5/(n+1)`）。所以往紧凑牌组里塞一张低于均值的牌**得分为负**，Smart 及以上会真的跳过——而旧逻辑是"只跳过负分牌"，而旧分几乎是恒正的，等于从不跳过。
- **角色饱和**取代了固定的牌组张数上限。按结构化角色（damage/aoe/block/draw/energy/vulnerable/weak/strengthdown/poison/doom/scaling/power/exhaust）统计已有张数，角色越挤这张牌越不值钱（`<4 → 1.0`、`<7 → 0.7`、`≥7 → 0.4`）；缺角色仍有明确奖励（唯一 AOE / 唯一过牌 / 唯一能量）。**不再设"最优牌组张数"**。
- **真实升级差值**。用**隔离副本**跑游戏自己的升级路径，比较费用、伤害、格挡、抽牌、能量、**能力数值增量**（如力量 2→3 这类升级此前完全测不到）、解锁的新角色和新关键词。降费按每点 14 分计——它能让一整条连招成立，不该和 +3 伤害同价。
- **删牌与拿牌同源**，并且显式保护"唯一防御/唯一 AOE/唯一能量源/唯一过牌"。同时保留一条**起手牌基线**：起手基础牌按定义低于曲线上（不然一个"整体都很差"的牌组会显示没有任何牌值得删）。
- **商店买牌**改用同一个增量（`GoldPerDeckValue=2.2` 统一换算成金币），不再用一套独立评分。
- 所有角色标签都从卡自身模型推导（DynamicVars + keywords），**没有引入任何外部评分表**；prototype/费用/稀有度都不依赖顾问数据。

回归（5 条新增 + 2 条改写）：填缺角色值得拿；饱和角色在膨胀牌组里**低于跳过**；无候选能改善牌组时**选择跳过**；广泛升级胜过单纯面板升级；升级与"再加一张"是不同量；删牌命中稀释牌且保护唯一防御；12 张同名 Inflame 时"再加一张"被拒而"升级已有"仍值得。

**待办（后续步骤）**：步骤2 原型/协同加成（标签层已就绪，`undefined` 时逐字节回落）；步骤3 Kernel 可用率（上局 `plans` 仅 19.5%）。配置文件方案仍按你的要求搁置。

## 0.26.0

向 STS2-AiTeammate 学习的两项：未建模卡的**结构化兜底**、事件专用 handler 注册表。安装包：`outputs/CoopBots-v0.26.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.25.6 的全部修复。）

- **结构化兜底：未建模卡不再被整张丢弃**。此前内核把"OnPlay 无镜像"判为 boundary 直接跳过该卡，导致 **多人专属支援牌根本不可能被打出**——上一局 COORDINATE 触发 58 次、BLAZE 3 次。现在对**能精确描述的形状**改为按卡自身变量近似施加：
  - 形状限定为：Skill + 恰好一个 `PowerVar<T>` + 无伤害/格挡/抽牌/能量/HP/X 费变量 + 目标为自己或单个队友。不在形状内的一律**仍然拒绝**（如 Intercept 仍是 boundary），绝不对看不懂的牌瞎猜。
  - 顺带处理了两个细节：`Coordinate` 声明的是 `PowerVar<StrengthPower>` 但实际施加 `CoordinatePower`，靠"该 power 声明的 OriginModel 是本卡"精确识别；临时力量类 power 必须**同时施加配对的真实力量**，否则回合结束的恢复会把增益变成减益。
  - 这类动作一律标记为 `estimated`，**永远不会被当成已确认的斩杀/救援**。
- **事件：数据驱动 + 专用 handler 注册表**。
  - 通用评分改为**读游戏自己的数据**而不是猜字符串：选项附带的遗物走现有 `RelicValue`；选项 hover tip 里的卡若是诅咒/状态按永久牌组成本扣分、是能力牌加分。这样 `LOST_WISP` 的 CLAIM（键名里完全没有"curse"，实际塞一张 Decay）这类**不再被当成中性选项**。
  - 新增 `IEventSpecialHandler` 注册表：只处理"选项数据表达不出的取舍"。首个是 **REFLECTIONS**——`打碎`会复制整个牌组并加诅咒（正是上一局把三个 Bot 牌组从 28/31/30 变成 57/63/61 的那张），任何通用规则都看不出来，现在一律选"触碰镜子"。`TABLET_OF_TRUTH` 的既有特例也迁入注册表；handler 返回 null 即回落到通用评分，且保证不会卡住事件。
- 回归：Blaze/Coordinate 可打出且数值正确、形状外的牌仍拒绝、临时力量不会被恢复反转；Reflections 在两种选项顺序下都选镜子；带诅咒的选项必须输给不带诅咒的（撤掉诅咒扣分后测试立刻失败）。
- 未做真实联机或 A10 胜率实测。

## 0.25.6

斩杀信号修复：此前"可斩杀"判定把易伤算了两次，导致提示从不出现（且评分虚高）。安装包：`outputs/CoopBots-v0.25.6.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2~0.25.5 的营地、节奏、事件与空闲修复。）

- **为什么实机从没看到斩杀提示**：整局日志里 `BotCallout` **一次都没发出**；同期 `human-finish:60` 出现了 **17 次**——说明评分认定"真人能补刀"17 次，而提示一次都没触发。
- **根因**：两处条件不一致，且**易伤被算了两次**。
  - 真人手牌估值（`AttackPotential` → `Analyze` → 卡牌预览）走的是真实伤害钩子，**已经包含易伤加成**。
  - 但 `SoftFinish` 又对估值乘了一次 1.5。于是评分虚高：只要敌人身上有易伤，即便真人根本补不掉，也会白拿 60 分，可能诱使 Bot 放弃正常输出。
  - 而提示用的是另一套（不乘 1.5）的条件，所以那 17 次全是**虚报**，提示自然一次都不出现。
- **修复**：两者共用同一个 `HumanFinishTarget` 判定，杜绝再次分叉；并且只在"**本回合计划新施加的易伤**"上乘 1.5（开局就已带易伤的敌人，估值里已经算过，不再重复）。这同时修掉了评分的虚高。
- **UI 可见性**：斩杀请求不再混在状态栏文字里，改为**独立的大号金色标签**，放在协作面板最上方（19px、暖金色），持续 6 秒。理由：它是要玩家立刻做一件事，必须第一眼看到。
- **新增日志**：发出时记录 `CoopBots callout: ...`，下次实机可直接确认触发次数。
- 回归：易伤由计划施加时必须触发提示（且与评分一致）；开局已带易伤时**不得**重复计算（撤掉该保护后测试立刻失败）。
- 未做真实联机或 A10 胜率实测。

## 0.25.5

no-action 43.9% 已定位：那是"Bot 已耗尽能量、在等真人打完"的空闲搜索，现已直接跳过。安装包：`outputs/CoopBots-v0.25.5.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2/0.25.3/0.25.4 的营地、节奏与事件修复。）

- **结论：no-action 不是评分缺口，而是空闲**。按回合统计每一场的出牌数（`KAISER_CRAB_BOSS` 第 8 回合真人 31 张 / 三个 Bot 合计 3 张，第 11 回合 真人 6 / Bot 0；出牌序列末尾还有连续 30 张真人、0 张 Bot）。三个 Bot 每回合各出 2–3 张（3 能量打完）就进入空闲，而真人的引擎一回合能出 10+ 张——空闲的每一秒我们都在跑一次完整内核搜索（capture + 最多 300/600ms），答案恒为"无动作"。
- **修复**：当**所有可行动 Bot 都没有可出的牌、也没有可用战斗药水**时，直接跳过内核 Poll（连 capture 也省掉），交给下面的药水与结束回合逻辑。药水（含"能量药水解锁手牌"这类）会绕过该快路径，确保不误伤。
- **附带收益**：这 43.9% 的空闲搜索同时也是 `stale` 的主要来源（真人每出一张牌就让我们的搜索作废）与 capture 卡顿的主要来源，跳过它们能同时降低卡顿、无用 CPU 与 stale 计数。
- 新增 `CoopBots idle-skip: no playable card; skipped=N` 日志，用来验证该守卫的命中量。
- **关于"女王锁链（Bound）"的猜测不成立**：本局日志里没有任何 `ChainsOfBinding`/`Bound` 记录，且终局 6 个回合 Bot 每回合都正常出牌（B1 7/4/2/3/2/2，B3 4/4/2/3/2/2，B2 在第 3 回合阵亡后为 0）。43.9% 全部来自前面几场"真人长回合"的战斗。
- **顺带量化出的真正强度差距**：`KAISER_CRAB_BOSS` 12 个回合里真人出 127 张、三个 Bot 合计 87 张——折算到单个角色，真人每回合出牌是 Bot 的约 4 倍。这不是规划问题而是**引擎（过牌/能量/成长）构筑差距**，属下一阶段可优化项。
- 未做真实联机或 A10 胜率实测。

## 0.25.4

修复严重事件 Bug：Bot 会在"单选事件页"上连选两个选项（镜子事件把三个 Bot 的牌组整个复制了一遍）。安装包：`outputs/CoopBots-v0.25.4.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2/0.25.3 的营地与节奏改动。）

- **根因（实机日志复盘）**：`EVENT.REFLECTIONS` 只有一页、两个互斥的终局选项（触碰镜子 / 打碎=复制整个牌组并获得霉运）。三个 Bot 都先选了"触碰镜子"，**紧接着又选了"打碎"** → 三个 Bot 的牌组被整个复制（28→57、31→63、30→61）并各多一张霉运，日志里同时出现 3 次 `Tried to set event options after event was finished!`。这发生在第 3 幕的 FABRICATOR → SOUL_NEXUS 精英 → 试验体 BOSS → 女王 BOSS 之前，等于用"牌组一半是重复牌"的残废状态去打最后四场硬仗。
  - 机制：选牌是 fire-and-forget，被选中的选项在**异步动画期间**就被标记为 `WasChosen`，而该页的选项列表还没变。我们的过滤只挡住"同一个选项"，于是下一帧就选了**另一个互斥选项**。
  - 修复：`ShouldAnswer` —— 只要该页有任意选项已被选择（页面还在结算/动画中），就等待页面推进（新页选项是全新的）或事件结束，绝不在同一页选第二次。
  - 回归：构造 REFLECTIONS 页面，标记一个选项已选后，另一个选项必须不可再选。
- **新增 no-action 诊断**：`no-action` 日志现在附带 `nodes / boundaries / root / best` 分数，用来区分"当时无牌可打（空闲，无害）"和"模拟了牌但都没比不动更好（评分缺口，最强规划器被静默降级给旧规划器）"。
- **本局其它量化结论（供后续优化排序）**：全场 2165 次内核决策里 **plans 19.5% / no-action 43.9% / stale 26.8% / boundary 9.8%**——即约 80% 的时间最强内核并没有在指挥 Bot，最后一场 BOSS 也是 kernel 22 次 vs 旧 `team-beam` 23 次。另外每次搜索的 `capture` 中位数 **48ms**、p90 65ms、最大 **258ms**（不可中断的主线程卡顿，全场约 2165 次）。
- 未做真实联机或 A10 胜率实测。

## 0.25.3

节奏：等待与搜算不再串行（每个 Bot 动作快 200–300ms）+ 营地愈合权重下调。安装包：`outputs/CoopBots-v0.25.3.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。（本版包含 0.25.2 的营地改动。）

- **"玩家结束后 Bot 没变快"的定位（实机日志）**：ai 动作间隔 = **等待 + 搜算**，两者原本是**串行**的。
  - 日志证据：真人结束后 `search=146–190ms wall=609–625ms; stop=time-budget`；真人行动中 `search=93–106ms wall=312–328ms`。
  - 即：真人结束后每个动作 = 等待 200ms + 搜算 ~600ms ≈ **800ms**；真人行动中 = 1000ms + ~300ms ≈ **1300ms**。所以只是快了 1.6 倍，而不是回到 0.2 秒——0.2 秒是**深搜引入之前**的节奏。
  - 另外 600ms 的墙钟上限**从未跑满节点预算**（只展开 272–757 / 3072 个节点），因为搜索按 4ms/帧切片，600ms 墙钟只换来约 180ms 真实计算。
- **修复（无损，不降低搜索质量）**：把"上一次规划实际花了多久"从下一次等待里扣掉，让等待与搜算**重叠**，节奏变成 `max(间隔, 规划耗时)` 而不是 `间隔 + 规划耗时`。
  - 真人结束：800ms → **~600ms**；真人行动中：1300ms → **~1000ms**；旧规划器（规划很快）仍保持 200ms/1000ms 完整间隔，不会被压成连发。
- **新增失效分类遥测**：`kernel combat summary` 的 `stale=` 现在拆成 `root=`（世界真的变了，丢弃是必须的）与 `notification=`（只是战斗状态通知计数变化，属可避免的浪费）。实测那场 Boss 战 `stale=62`，下次跑一局即可知道其中多少是可避免的——如果 `notification` 占多数，可以安全放宽失效判定再省一大截。
- 若还想更快，可下调 `KernelCombatPlanner` 里真人结束后的墙钟预算（现 600ms → 约 300ms 可再省 ~300ms/动作，代价是展开节点数约减半）。
- 营地改动见 0.25.2 一节。
- 未做真实联机或 A10 胜率实测。

## 0.25.2

营地愈合权重大幅下调：半血以上不再照顾，且不再多人堆叠奶满。安装包：`outputs/CoopBots-v0.25.2.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- **问题**：营地 `MEND` 的评分是"缺多少血 × 2.0（真人权重）"。一个 20/73 的真人缺 53 血 → **106 分**，而 `SMITH` 只有 16 分。于是全队每个 Bot 都放弃锻造去奶，而且几个人奶同一个人 → 瞬间奶满，长期收益全丢。
- **现在的规则**：
  1. **半血以上完全不照顾**。只把"补到 50% 为止"的那部分血量计入价值，超过部分一律 0 分。满血/健康时愈合得 0 分，Bot 直接锻造。
  2. **越接近死亡越贵**。按 `urgency = (0.5 − 当前血量比) / 0.5` 做二次加权（`×(1 + 2·urgency²)`），所以濒死队友依然会被优先救。
  3. **单位血量价值调低**（每点 0.8，真人 ×1.2）。综合曲线后：约 **三成血以上 ⇒ 锻造**；两成血以下 ⇒ 愈合明显胜出。
  4. **同一营地不堆叠愈合**。Bot 在同一线程上依次决策，会把"前一个 Bot 已经承诺的奶量"记进目标的有效血量，后面的 Bot 看到队友已被奶回半血以上就改为锻造——不会再有"几个人一起奶满"。
- **效果（20/73 场景）**：只有 **1 个** Bot 会出愈合（把真人奶到约 57%），其余 Bot 全部锻造，不再出现奶满 + 全员放弃锻造。
- 回归：半血以上必须选锻造；接近死亡必须选愈合；两个 Bot 中第二个不得重复奶同一个人（关掉预留机制后该测试立刻失败）。
- 若你想要更贪心（例如只在 35% 以下才奶），改 `RestSitePatches.HealPerHp` / `MendSafeFraction` 即可。
- 未做真实联机或 A10 胜率实测。

## 0.25.1

回合性降低敌方输出的卡牌建模（尖啸/黑暗镣铐/弱化之触/星灭等）+ 团队防溢出。安装包：`outputs/CoopBots-v0.25.1.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- **"本回合降敌方力量"类卡牌此前在旧规划器里等于不存在**。尖啸(PiercingWail)、黑暗镣铐(DarkShackles)、弱化之触(EnfeeblingTouch)、星灭(DyingStar)、以及 CrushUnder/Mangle 的效果变量名是 `StrengthLoss`，而旧评分只认 `StrengthPower`/`WeakPower`/`VulnerablePower`，于是这些牌被算成"无效果技能"（`facts 全 0` → 走"无价值牌"分支），**几乎永远不会被打出**。
  - 现在新增 `CardFacts.StrengthDown`，从 `StrengthLoss` 变量读取（凌虐 Malaise 走 X 费另算），并按"每次受击少 N 点 × 剩余攻击次数 × 存活队友数"估值（以敌人实际威胁为上限，避免把已残废的敌人估高）。只要敌人在攻击，就按高于格挡的权重加分，**优先于格挡打出**。
- **团队防溢出（跨 Bot / 跨真人）**。旧团队规划器只建模了虚弱（`WeakIncoming`），不知道队友手里的降力量牌会让本次敌方阶段整体变小，于是会出现"某个 Bot 抽到尖啸、其他 Bot 仍在叠防"的浪费。现在 `TeamCombatPlanner` 新增 `EnemyStrengthDown`（按敌人累计、可叠层、被神器 Artifact 消耗），在评估用"每点降力量 × 该敌人攻击次数"直接扣减该敌人的来袭伤害。
  - 回归：同一手牌下，加入尖啸后规划器改打尖啸而不是格挡；降力量已生效（来袭为 0）时规划器不再浪费格挡。对该扣减做了反证（关掉后测试立刻失败）。
- **修正一个过度估值**：跨回合预测（未来回合威胁）此前用分支状态计算，会把**本回合就会恢复**的临时降力量算进下一回合，等于凭一张一次性牌减少未来伤害。新增 `KernelSession.RestorableStrength`，在预测未来回合时把临时力量变化加回，避免"本回合已安全还硬打尖啸"。
- 真人侧建议同步：`HumanCoopAdvisor` 的铺垫估值与起手白名单加入这批牌，真人看到的出牌建议也会优先推荐它们。
- 说明：内核搜索评估对"敌方攻击"采用保守模型（视作打向每名队友），因此降力量牌在内核里本身就严格优于单体格挡，未额外加"起手排序"规则；顺序问题由"每次只提交一个动作再重规划"自然解决。
- 未做真实联机或 A10 胜率实测。

## 0.25.0

搜索利用率优化 + 真人配合强化（取消"真人阵亡最高优先级"、Bot 主动呼叫斩杀）。安装包：`outputs/CoopBots-v0.25.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- **搜索利用率：状态去重 + 分类保底**。光束前沿此前按分数截断，会把同一局面下大量同质分支（如同一张牌的不同目标）挤满配额。现在先按 `CompactStateKey`（每人 HP/格挡/能量/就绪/手牌、敌人 HP/格挡、全量能力、回合数、敌方阶段状态）**去重取最优**，再按 `attack/skill/power/potion/endturn/other` **每类保底一条**，最后按分数补足宽度。避免"看起来搜了很宽，实际只探索了一种牌"。
- **移除真人死亡的"最高优先级"**。以前真人阵亡带 `-1e9` 硬惩罚、真人死亡是评分首键，等价于"只要真人快死就无脑保人"，牺牲团队节奏。现在**真人/Bot 死亡代价一致**（`40 + 10%最大生命`），仍然按团队收益最大化决策；仅保留"全队团灭"（-1e8）与"胜利"（+1e7）两个硬层级。
- **Bot → 玩家主动呼叫斩杀**。当内核判定"目标本回合能被斩杀、但需要真人的输出才能补足剩余血量"（`HumanCover ≥ 剩余HP` 且 Bot 无法独力完成）时，Bot 会通过新增的 `BotCalloutSync` 网络消息在**所有真人屏幕上**弹出提示，例如"请对 XX 输出，本回合可斩杀（约需 N 点）"，持续 6 秒，同时显示在协作面板状态栏。这是对原生仅有 Ping 的补充，用于在实机上把"差一点斩杀"变成"真的斩杀"，降低战损。
  - 呼叫只在**真人确实能补足**时发出，不会为 Bot 自己能杀的敌人呼叫；Vulnerable 会按 1.5 倍计入真人的可贡献伤害。
  - 同一提示 4 秒内去重，避免刷屏。
- **真人收尾软项改为"在射程内"的固定加分**（上限 120×置信度）。此前按 `min(剩余HP, 真人可贡献)` 连续计分，导致**不行动反而分更高**（剩余血量越多软分越高），搜索会拒绝出牌；现在只要敌人进入真人斩杀范围即给固定分，攻击与否由真实的敌人血量项 `×0.35` 决定。
- 回归：呼叫用例（Bot 6 伤、真人 3×6 覆盖 18、敌人 17 HP 时必须发出 `NeededHp==11` 的呼叫）；全队团灭层级仍最高；真人阵亡不再单独定价。
- 已知后续项：选牌覆盖仍有限（每卡最多 8 分支）、保留/X 费跨回合细节、局外边际价值（P2-1）未做。
- 未做真实联机或 A10 胜率实测。

## 0.24.2

修复"真实多人下内核无法模拟任何格挡牌" + 战斗复盘日志。安装包：`outputs/CoopBots-v0.24.2.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- **根因（由实机日志复盘定位）**：多人战斗带有 `MultiplayerScalingModel` 作为格挡乘算钩子，而内核里的处理器对 `playerCount != 1` **直接抛 `NotSupportedException`**（上游是单人求解器）。结果**每一张格挡牌在真实多人下都会抛异常 → 被判为边界 → 内核完全算不了防御**；日志里表现为大量 `DEFEND_SILENT:prediction-exception:NotSupportedException`，防御回合退回旧规划器（`team-beam`），攻击用内核（`kernel-team`），防守决策因此割裂——这与"一层尚可、二层精英团灭"的现象一致。测试未暴露是因为测试战斗没有这个模型。
  - 修复：照搬原生缩放（玩家格挡不受缩放影响；敌人格挡按人数与幕数缩放），不再抛异常。
- **战斗复盘日志**：新增 `CoopBots combat start: <遭遇> act=N; 每人 HP/上限 与牌组张数`；每场战斗结束的 `kernel combat summary` 现在附带 `encounter / maxRound / party`，可以直接从日志复盘团灭。
- 回归：3 人战斗（带 `MultiplayerScalingModel`）中格挡牌必须可出且格挡值不被缩放。
- 已知后续项：日志里 `end-turn:round-pending-choice` 出现频繁，说明跨回合推进常被"待选择"挡住（选牌覆盖不足），属 P1-2/P1-3 范围。
- 未做真实联机或 A10 胜率实测。

## 0.24.1

路线精英估值按幕数与构筑成熟度分级。安装包：`outputs/CoopBots-v0.24.1.zip`；游戏v0.111.0。

- 第二层+未成型构筑的精英分大幅下调；血量 <70% 恒为强负分；地图建议追加警告。

## 0.24.0

P1-1 四项修正 + P1-2 跨回合可执行搜索。安装包：`outputs/CoopBots-v0.24.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- **P1-1 四项修正**：
  1. **结算后不再叠加根预测**：多回合预测只在"未结算且尚未推进回合"时计入。此前敌方阶段已结算的节点仍在用根时刻的预测，威胁错位一轮，而结算节点恰被优先采用。
  2. **未建模的非攻击怪招改为边界**：不再抛异常；此前会被外层捕获并**整场停用内核**，多怪/Act3 很常见。
  3. **结算计划不再无条件优先**：仅当结算节点分数不劣于最优卡牌线时才采用，避免"为了结算而结算"。
  4. **回合结束分支保留探索位**：开启 EndTurn 搜索时至少保留一条结束回合的路线，防止它被卡牌线在剪枝时挤掉。
- **P1-2 跨回合可执行搜索**：回合结束并结算敌方阶段后，按预算推进**下一回合的玩家回合开始**——回合数、回合开始遗物/能力、清格挡、能量重置、抽牌、自动前置出牌——然后清空就绪状态，搜索继续在第二回合出牌。回合上限由搜索选项控制（默认 2），到达上限时以敌方结算为终止节点。
- **取数口径**：第一回合沿用实况意图（保持既有评分口径）；已推进到第二回合时改用**分支自己的怪物 AI**；根预测只在第一回合、未结算前有效。
- 回归：结算后推进第二回合、预测不复用、回合上限终止、未建模怪招走边界、结算不覆盖更优卡牌线（5 条新用例）。
- 边界：选牌覆盖仍有限（每卡最多 8 分支）；状态去重/多样性保留（P1-3）、保留/X 费跨回合细节、局外边际价值（P2-1）未做。
- 未做真实联机或 A10 胜率实测。

## 0.23.0

第一批（可信度/性能）+ 第二批（硬结论风险分级、路线状态推进）。安装包：`outputs/CoopBots-v0.23.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- **搜索墙钟截止**：真人行动中 300ms、真人全结束后 600ms，到点先交付可用计划。
- **真人收尾软项连续计分**（按可贡献 HP，上限 120），消除跨阈值跳变。
- **回退遥测**：每场战斗输出一次 `plans / boundary / no-action / stale / exception`。
- **正常局面变化不再停用内核**：中途根变化→本 tick 交旧规划器、下 tick 重试；只有真正异常才停用。
- **硬结论风险分级**：带环境未知的计划标记 `estimated`，且不再声明"已确认避免死亡"。
- **路线资源状态推进**：整幕 DP 携带血量/金币档位并在经过房间时更新。
- 未做：P1、P2-1、真人手牌估值缓存。

## 0.22.2

修复未来威胁被重复折扣 + 统一测试产物。安装包：`outputs/CoopBots-v0.22.2.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- **未来威胁被折扣两次（重要）**：`nextIncoming` 累计时已乘 `NextTurnThreatWeight=0.25`，加入战损时又乘了一次，第 1 个未来回合实际系数是 **0.0625**（比预期弱 4 倍）。现在只折扣一次。
- **构建门禁**：内核回归此前并非测试将要打包的 `CoopBots.dll`；现在两类测试都针对打包产物。
- 回归：单回合 100 点裸伤必须恰好产生 25 分代价（钉住 0.25）。

## 0.22.1

修正 Cook 语义与休息点锻造判定。安装包：`outputs/CoopBots-v0.22.1.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- **Cook 语义写反了**：`CookRestSiteOption` 实际是**删除两张牌并 +5 生命上限**（`CreatureCmd.GainMaxHp(..., 5)`）；上一版误当成"删两张、-5 上限"的代价型选项，会错误回避它。现在估值 = 两张最差牌的可删价值 + 10（+5 上限的耐久收益）；若两张最差牌本身不错（可删价值为 0），则只剩 10 分，不会为了 +5 上限拆好牌。
- **顺带修掉一个更基础的 bug**：休息点判断"是否值得锻造"用了 `CardValue ≥ 22`，但 `CardValue` 含**重复牌惩罚**（牌组里每张同名牌 -5，连它自己也算），于是重复牌多的牌组里几乎所有牌都低于阈值，**Bot 几乎从不锻造**。新增 `UpgradeValue`：在判断"已有牌是否值得升级"时剔除重复与稀释惩罚。现在重复牌多的牌组也会正常锻造。
- 回归：Cook 在垃圾牌组被选（删 2 张 + 加上限）；好牌组拒绝 Cook；重复牌多的牌组中"该牌不值得再加一张、但值得升级"成立。
- 未做真实联机或 A10 胜率实测。

## 0.22.0

营地（休息点）策略：遗物感知 + 团队治疗 + 特殊选项。安装包：`outputs/CoopBots-v0.22.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- **改用稳定的 `OptionId` 评分**（HEAL/MEND/SMITH/DIG/LIFT/COOK/HATCH/KINDLE/CLONE），不再用类名子串匹配。
- **修补漏掉的特殊选项**：铲子（DIG）、壶铃（LIFT）、COOK、HATCH、KINDLE、CLONE 此前全部落到默认分（低于治疗/锻造），**实际从不会被选**。现在按效果给分：挖掘按剩余幕数递减（越早越值）、举重随剩余幕数递增（永久力量）。
- **遗物感知**：治疗量改用 `HealRestSiteOption.GetHealAmount` / `MendRestSiteOption.GetHealAmount` 读取**真实值**（枕头、羽毛等自动生效）；捕梦网、小邮箱、尊贵茶具/仿制茶具提升"休息"价值。
- **团队治疗**：`Mend` 目标已由自动应答选"血量百分比最低的其他队友"，但评分没体现团队需求；现在按队友缺口 × 权重（真人 2.0 / Bot 1.4）评分，队友重伤时压过自我治疗。
- 边界：`MiniatureTent` 只影响选项可用性；`Mend` 目标不含自己。

## 0.21.2

修复成长型敌人（如钙化邪教徒）被集火忽略导致团灭。安装包：`outputs/CoopBots-v0.21.2.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- 问题：集火只看"剩余血量"，于是选了低血杂兵；而多回合威胁投影用的是**当前力量**，没有把 Ritual 这类**每回合成长**算进去。结果成长敌人被放着长力量，前期防御牌压不住，最终团灭。
- 修复一：**集火改为按有效威胁**——`(血量+格挡) / (1 + 每回合成长 × 0.8)`。成长敌人即使血更厚也会优先；没有成长时仍按血量选最低。
- 修复二：**多回合投影外推成长**——第 N 个未来回合的来袭按 `基础伤害 + 每回合成长 × N` 计算。
- 修复三：**评分新增"复合威胁"项** `成长 × 12 × 存活人数 × 剩余血量占比`，随敌人掉血线性下降、被击杀清零。权重刻意选成"对成长敌人 1 点伤害 ≥ 挡住 1 点伤害"，让"尽快输出"压过"先叠甲"。
- 回归：集火优先成长敌人；无成长时仍优先低血；评分计入成长（-80.0 vs -39.5）；**最优计划必须攻击成长敌人，而不是只用防御**。
- 未做真实联机或 A10 胜率实测。

## 0.21.1

修复路线估值：问号房间不再劣于普通战斗。安装包：`outputs/CoopBots-v0.21.1.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- 问题：原估值里普通战斗 12 分高于问号 10 分，于是「5 小怪」会胜过「3 问号 + 2 小怪」。
- 修复：节点估值改为**问号优于普通战斗**，并按**幕数**与**牌组大小**调整（问号：第 1 幕 22 / 第 2 幕 16 / 第 3 幕 13；战斗：9 / 7 / 5，牌组 <15 张 +4，低血额外扣分）。
- 回归：第 1 幕「3 问号 + 2 战斗」胜过「5 战斗」；低血时战斗价值下降；后续幕问号仍不低于战斗。

## 0.21.0

路线规划：整幕最优路径 + 地图上本地画线推荐。安装包：`outputs/CoopBots-v0.21.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- **整幕路径搜索**（`RoutePlanner`）：把幕地图当作从当前位置到 Boss 的 DAG，用「节点价值 + 0.92 × 最优子节点价值」做记忆化动态规划。因此能看到后续铺垫，而不是只比下一个房间。节点价值考虑队伍最低血量、你的金币、有购物预算的队员人数（休息/商店/精英/宝箱/普通战斗/事件/先古）。
- **地图画线推荐**（`MapRouteOverlay`）：在地图界面用游戏同款线条风格画出金色推荐路线；路线变化时更新，离开地图时清除。
  - 按你的选择采用**本地覆盖层**：各端各自本地计算并绘制，**不污染真人的笔迹、不发网络消息、可随时更新/清除**。
- 地图面板文字建议同步改为整幕路径的前 3 步（例如「休息点 → 商店 → 精英」）。
- 回归：分支择优到 Boss；**看穿下一步**（较差的当前房间 + 更好的后续，胜过较好的当前房间）；已在 Boss 的边界。
- 说明：覆盖线的定位基于地图点节点的全局坐标换算，实机观感需你确认；若不理想，可改为直接调用游戏的同步画笔（代价是会与真人笔迹混合）。
- 未做真实联机或 A10 胜率实测。

## 0.20.0

P1：构筑与商店估值（O01–O03）。安装包：`outputs/CoopBots-v0.20.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- **选牌估值重写**（`HumanCoopAdvisor.CardValue`，驱动奖励选牌、商店买牌、升级选择与删牌选择）：按已建模数值加权——伤害、格挡、抽牌、即时能量、易伤/虚弱、力量/敏捷/专注成长、毒/灾厄——再叠加稀有度、费用效率与升级状态；最后按**牌组需求**修正：防御缺口、前期输出不足、缺少成长能力、缺少群体伤害、缺少过牌；并对**超大牌组**中的普通牌施加稀释惩罚（该跳过就跳过）。
- **商店修正**：药水白名单此前写错模型名（`ExplosivePotion`/`FearPotion` 实际不存在），导致这两类**永远不会被购买**；已改为真实模型名并补齐；买牌门槛提高，避免买填充牌。
- **休息点**：只有存在值得升级的牌（估值 ≥22）才考虑锻造，血量偏低更倾向治疗。

## 0.19.1

P1：不再因"环境未知"而不敢出牌 + Boss 反应机制回归。安装包：`outputs/CoopBots-v0.19.1.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- **修掉一个致命判定**：内核原来对**任何**未补偿的预测缺口都判定该出牌失败，于是该牌会从搜索中消失。当敌人带有未建模的被动/反应 Power（或未建模遗物钩子）时，Bot 会**不敢攻击**这类敌人——第三层精英/Boss 常见。现在改为：**只有"牌自身 OnPlay 语义未建模"才拒绝该牌**；环境类未知（敌方 Power、遗物钩子等）降级为估算，攻击照常参与规划。药水的 OnUse 同理。
- **Boss 反应机制验证**：新增回归确认尖刺（Thorns）在内核中被正确结算——攻击带尖刺的敌人仍可出牌，攻击者承受反伤（实测攻击者 80→77、敌人 50→44）。
- 内核此前已覆盖多数 Boss 机制，本轮加测试锁定：尖刺（`BeforeDamageReceived`）、复活/多阶段（Adaptable/Reattach/Illusion/Infested/Stock/Surprise 的死亡处理）、召唤/援军（Minion/DeathPowerSupport）、神器层数。
- 仍未完成：完整敌方回合动作搜索（引擎单目标假设，见上一版说明）。
- 未做真实联机或 A10 胜率实测。

## 0.19.0

P0-2 双阶段调度与多回合威胁投影。安装包：`outputs/CoopBots-v0.19.0.zip`；游戏v0.111.0，所有真人一起更新。

- 真人在行动时保持紧预算回合内搜索（9/8/768，约200ms）；真人全部结束回合后切换深搜（16/12/3072，约1200ms）。
- 评估从"只看本回合意图"扩展为未来多回合敌方攻击（0.5 逐回合衰减；未结束看 2 回合，全结束看 4 回合）。
- 未完成：完整敌方回合动作模拟被 `MonsterMoveSemantics.ApplyForecastMove` 的单目标假设阻塞。

## 0.18.0

药水并入内核联合搜索（S07）。安装包：`outputs/CoopBots-v0.18.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- 药水不再是"出牌之外的单点判断"，而是和卡牌一起进入内核团队搜索的动作：`KernelTeamSearch` 会枚举每名 Bot 的可用药水与合法目标，用同一评分与预算求解。因此求解器能自己发现"先喝力量药 → 再打两张打击"这类序列，也能比较"喝在谁身上"。
- 部署仍是"只执行首个动作、下一步重规划"：若最优计划的首个动作是用药水，则作为**免费动作**执行，随后按真实局面重新规划出牌。
- 救援药水仍优先（`BotPotionPlanner` 先行）；有救援时不冒险用药。药水只在能提升团队结果时被采纳（提升来自搜索评分本身）。
- 不能模拟的药水在分支中被跳过，不影响其他动作；药水在同一分支内用过即从该分支消失（不会重复使用）。
- 回归：两张打击共 12 点、敌人 14 血时，只有"力量药→打击→打击"（16 点）能斩杀，规划器必须把药水排为首个动作且序列长度为 3；敌人 12 血时纯出牌已能斩杀，必须**保留**药水不浪费。
- 未做真实联机或 A10 胜率实测。

## 0.17.0

主动药水（确认收益才用）。安装包：`outputs/CoopBots-v0.17.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- 原逻辑问题：药水只在"有人即将死亡"时才评估（`BotPotionPlanner` 无濒死即返回 null），只建模 5 种救急药水，且与团队计划**二选一**。增益/能量/易伤类药水没有任何使用路径，所以实战中几乎不喝药。
- 本轮：内核新增主动药水的 OnUse 镜像（StrengthPotion、DexterityPotion、FocusPotion、VulnerablePotion、EnergyPotion），`KernelSession.UsePotion` 可在模拟分支中使用药水（自动映射到模拟克隆并结算力量类数值变更）。
- 规划器对候选药水 **fork → 用药 → 短搜索**，**仅在确认收益时**采纳：新达成击杀，或减少团队死亡。上限 6 个候选、每个约 30ms；未建模药水自动跳过，绝不凭估计喝药。
- 运行时把它当**免费动作**：先用确认药水，下一次行动再按真实局面重规划出牌。目标可选任意队友（含真人），由"谁能把收益放大最多"决定。
- 回归：力量药水把 7 血敌人从"打 6 打不死"变成"打 8 确认斩杀"时，必须被检出且指向施法者。
- 剩余：药水尚未与出牌序列**联合求解**（喝能量药→接连招、给保护→全资源爆发），属 S07；当前是保守的"确认收益"版本。
- 未做真实联机或 A10 胜率实测。

## 0.16.0

P0 强度优化：让内核真正上场 + 跨回合威胁。安装包：`outputs/CoopBots-v0.16.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- **P0-1 未建模卡不再整场禁用内核**：以前只要搜索中任意一张手牌产生预测缺口，就停用内核打完整场、退回旧启发式规划器。A10 手牌几乎必然含未建模卡，导致强引擎实际几乎不运行。现在改为"仅该卡不参与搜索"，其余已建模卡继续由内核规划（面板日志会显示 `partial plan (skipped unmodeled)`）。
- **P0-2 跨回合信息**：接入上游 `IntentForecaster`，在评估里加入**下一回合敌方攻击**（按当前分支的力量/虚弱等重新计算）。这样"先杀/先虚弱将要打出重击的敌人"会被正确偏好，而不是只看本回合意图。完整敌方回合动作搜索未做——引擎的 `AdvanceRound` 是私有且绑定单人求解器状态，无法安全复用（见 INTEGRATION.md）。
- **P0-3 多回合累计评估**：下一回合来袭伤害按 0.25 权重计入团队战损（该伤害落地前全队会有新回合、抽牌与格挡，故为折价威胁信号而非实际损失）。
- 新增回归：手牌含未建模卡时内核仍须给出 `Ready` 计划（而非整场禁用）；下一回合威胁必须使团队评分变差。
- 未做真实联机或 A10 胜率实测。

## 0.15.7

定位到"Bot 完全不出牌"的真因：内核程序集加载上下文错误。安装包：`outputs/CoopBots-v0.15.7.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- **日志证据**：`godot.log` 反复出现
  `System.MissingMethodException: Method not found: 'System.String CoopBots.Kernel.KernelSession.CaptureLiveStamp(MegaCrit.Sts2.Core.Combat.CombatState)'`，
  栈顶是 MonoMod 的 JIT 钩子，调用链 `KernelCombatPlanner.Poll → BotRuntime.Tick`。**这是 JIT 期异常，`Poll` 自己的 try/catch 接不住**，异常直接冒到 `Tick` 外层，导致整个 tick 在"回退旧规划器"之前就中断——所以 Bot 一张牌都不打（也解释了为什么之前的规划器兜底没生效）。
- **真因**：内核 DLL 本身没问题（已用元数据核对 `CaptureLiveStamp` 确实存在，部署文件与包内文件哈希一致）。问题是 `KernelAssemblyLoader` 把内核加载进了 `AssemblyLoadContext.Default`，而本 Mod 由游戏的加载器载入；两个上下文各自解析 `sts2.dll`，于是内核签名里的 `CombatState` 与游戏传入的不是同一个类型标识 → JIT 找不到匹配方法。
- **修复 1**：内核改为加载进**与本 Mod 相同的 AssemblyLoadContext**，类型标识统一；加载时额外反射校验 `CaptureLiveStamp(CombatState)` 签名，缺失就在启动阶段明确报错，而不是在战斗首帧崩溃。成功后日志会打印 `CoopBots kernel loaded into '<上下文>'`。
- **修复 2（兜底）**：`BotRuntime` 对 `KernelPlanner.Poll` 单独 try/catch。即使内核因任何原因（JIT/加载/运行时）失败，也只把本次当作 `Fallback`，继续走旧规划器，绝不再中断整帧。
- 未做真实联机或 A10 胜率实测。

## 0.15.6

"Bot 不出牌"继续修复：团队主动等待不再无限期。安装包：`outputs/CoopBots-v0.15.6.zip`。

- 核查内核与多人的兼容性：未发现会导致调用失败的单机硬依赖。
- `TeamCoordinator` 在团队规划器弃权时只放行"未建模特殊牌"，普通卡被全部过滤 → 运行时新增 6 秒超时接管，并让特殊牌回退合并而非清空普通候选。
- 加上在途搜索不被瞬时忙碌打断、选中牌失效不再静默 return，共三道兜底。

## 0.15.5

修复"整场战斗 Bot 不出牌"（在途搜索被打断）。安装包：`outputs/CoopBots-v0.15.5.zip`；游戏v0.111.0，所有真人一起更新。

- 内核搜索跨帧展开，但运行时在"动作队列非空/有动作执行"的瞬时忙碌帧上 `KernelPlanner.Reset()`，导致搜索永远推进不完、每帧 Pending、整场不出牌。现在瞬时忙碌只跳过本帧，计划是否过期由规划器按实况根校验。
- 选中牌提交前失效时不再静默 `return`，改为记录、重规划，必要时结束该 Bot 回合。

## 0.15.4

修复"整场战斗 Bot 不出牌"的卡死（搜索完成后的失效分支）。安装包：`outputs/CoopBots-v0.15.4.zip`；游戏v0.111.0，所有真人一起更新。

- 搜索完成后若实况根变化或首个动作校验失败，原会 `Reset()` + `Pending` 并无限循环；现在完成后只返回 Ready（必带计划）或 Fallback，无推荐动作交回旧规划器，实况连续两次失效即本场停用内核。
- 运行时防御：内核没有可用计划时一律走旧规划器收集候选。

## 0.15.3

技能触发的敌方强化机制核查（实验体/与我一战）。安装包：`outputs/CoopBots-v0.15.3.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- 实验体 Boss 的 Enrage：任一角色打出技能牌，Boss 即获得力量（普通+2 / A10+3），之后对**所有人**的伤害提升。内核已镜像该钩子，敌方力量按成员结算。
- 实测：技能牌触发 Enrage 的团队代价 **3 人 6.0 vs 1 人 2.0**，随人数线性放大。
- 与我一战（FightMe）：持有者+3力量、敌人+1力量；敌方力量走同一路径，全队负面已计入。
- 已知边界：单回合搜索不推演力量未来累积；敌人本回合非攻击意图时，Enrage 叠加的力量本回合无直接代价。

## 0.15.2

敌我增益类遗物的团队计价。安装包：`outputs/CoopBots-v0.15.2.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- 核查对象：给敌人加力量的遗物（Brimstone、PhilosophersStone）。二者的收益都只归**持有者**，但敌人获得的每点力量会让**全队每个人**多挨伤害。
- 战斗内：内核已正确实现 Brimstone（持有者+2、每名存活敌人+1），敌方力量按成员计入团队战损。
- **本轮补全**：局外估值此前把这两件遗物当作"未知，0 分"，现按净团队收益估值——个人收益封顶、敌方力量按全队人数计负面，人数越多分值越低，商店/宝箱/奖励与真人建议据此回避。
- 实测回归：Brimstone/PhilosophersStone 在 3 人队的估值低于 1 人队；团队正向遗物 RedMask 相反。

## 0.15.1

共享减益的团队价值核查与真人易伤补全。安装包：`outputs/CoopBots-v0.15.1.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- 核查结论：内核团队评分对共享减益是**按全体成员**计价的，而非单人。评分对每名存活成员分别估算来袭伤害并求和，因此虚弱/减力量每减少 1 点来袭伤害，都在全队每个成员身上各记一次。
- 实测（受控局面，敌人固定攻击 10）：虚弱增益 **3 人队 9.0 vs 1 人 3.0**；减力量 3 人 40.0 vs 1 人 20.0。
- 易伤：内核搜索在同一共享分支内模拟所有 Bot 的后续攻击，先易伤再让每个队友出手优于先出手（受控局面敌人剩 34 < 40）。**本轮补全**：易伤对**真人**后续攻击的放大此前未计入，现在敌人处于易伤时真人收尾射程按倍率放大。
- 边界：来袭伤害按"每个敌人攻击命中每名存活成员"建模，与既有规划器一致。

## 0.15.0

多人卡牌按难度分级并建模简单卡。安装包：`outputs/CoopBots-v0.15.0.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- 分级依据：卡的队友语义是否已由内核现有钩子承载。**A 级（简单，本轮建模）**：EnergySurge、BelieveInYou（即时给队友/目标回能）、Sneaky、BeaconOfHope（Power 钩子 `AfterCardPlayed`/`AfterBlockGained` 内核已有，只缺 OnPlay 施加）。加上此前可解的 TagTeam、GangUp、Rally、Mimic、HuddleUp，**19 张协同卡中 12 张内核可解**。
- **B/C 级（放弃，保持边界回退）**：Flanking、OneForAll、Intercept 需要内核缺少的伤害加成/伤害重定向钩子；Concoct、Tutor 需要选牌交互；Knockdown 的 Power 加成伤害同样无钩子（只应用 Power 会低估，故不启用）；DemonicShield 需要 CalculatedBlock + 队友格挡结算。这些卡保持"显式边界→整场回退旧规划器"，**不会静默按 0 计价**。
- 实现方式：在内核 `CardEffectSpecRegistry` 增加受保护的适配，`scripts/import-combatsolver-kernel.py` 同步更新并在锚点变化时报错；已验证重新导入可逐字节复现（除行尾）。
- 效果验证：EnergySurge 给每个存活队友 +2 能量（施法者净 +1，扣 1 费）；BelieveInYou 只给目标队友 +2；Sneaky/BeaconOfHope 正确施加 Power；`Rally` 对全体队友加格挡。

## 0.14.2

多人卡牌在内核中的取值审计与安全修复。安装包：`outputs/CoopBots-v0.14.2.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- 背景：内核来自单人最优解求解器，需确认多人卡牌不会被错误计价。审计发现内核引擎层本身是队伍感知的（`GetTeammatesOf`、`AnyAlly`/`AllAllies` 目标、按生物的攻击历史、按所有者隔离的资源），并非只用单人假设。
- 修复崩溃：敲倒（Knockdown）的上游镜像会抛 `NullReferenceException`，原会中断整个搜索。现在 `KernelSession.Play` 对任何镜像异常**失败关闭**，返回 `prediction-exception` 边界并回退，不再让单张卡毒化搜索。
- 边界命中后**整场停用内核**，避免每个动作重复执行注定失败的昂贵搜索；该战斗交给多人感知的旧规划器。
- 剩余风险：`MODELED` 只表示上游未报告预测缺口，不等于已逐张做原生差分验证多人语义；尤其 TagTeam 的"下一张其他队员攻击重复出牌"仍是通用出牌计数镜像，尚未单独核验。

## 0.14.1

修复"合力可斩杀却先打防御"的问题。安装包：`outputs/CoopBots-v0.14.1.zip`；游戏v0.111.0，所有真人一起更新。**运行依赖：RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295）。** 玩家建议保持关闭。

- 内核团队评分新增"真人可收尾"软项：估算真人手牌本回合还能对每个敌人打出多少伤害（复用 `TeamCombatPlanner.AttackPotential`，即 `GeniusCombatStrategy` 数值 + 能量背包）。当某个敌人当前的剩余生命已进入真人打击范围时，给该分支一个加分，使"打伤害把敌人送进真人斩杀线"优先于"先叠格挡"。
- 约束：只对 **Bot 自己无法独立斩杀** 的敌人生效（Bot 能独自斩杀时仍由确认斩杀/清场项决定，不会被软项拖去让真人收尾）；只记一名最容易收尾的目标；真人被打击后 Bot 面临死亡时该加分降到 25%，保留生存优先；纯加法，**不会**像旧版那样反过来压低 Bot 自己的攻击。
- 该软项不改变"确认斩杀/清场"判定，仍不把真人输出算作确定击杀；真人死亡的最高优先级、Bot 死亡代价与流沙即死保护不变。
- 内核面板日志在触发时追加 `human-finish:<分值>`，便于观察。
- 已知边界：真人输出仍是软估计，若真人实际没有收尾，Bot 可能已放弃部分格挡；这是为抢斩杀主动接受的取舍。未做真实联机或 A10 胜率实测。

## 0.14.0

CombatSolver 模拟内核接入战斗决策。安装包：`outputs/CoopBots-v0.14.0.zip`；游戏v0.111.0，所有真人一起更新。**本版新增运行依赖：必须安装 RitsuLib 0.5.20 及以上（Steam 创意工坊 id 3747602295），否则 Mod 拒绝加载。** 玩家建议保持关闭。

- 新增 `CoopBots.Kernel` 实验项目，导入 CombatSolver 0.33.9 的 Engine / Prediction / Search 共 267 份源码，命名空间改为 `CoopBots.Kernel.Vendor`，逐文件来源记录在 `UPSTREAM.json`。不加载原 Mod 的启动、界面、自动部署或联网功能。
- 战斗出牌改为**内核优先**：`KernelCombatPlanner` 在游戏线程捕获全队共享战场，`KernelTeamSearch` 交错搜索所有 Bot 的动作，卡牌/遗物/Power/牌堆/资源结算全部由上游引擎完成；抽到或生成的牌会重新进入候选。
- 团队评分沿用既有策略：真人死亡最高优先、Bot 死亡代价按 40 + 生命上限 25% 计、集火记忆、流沙即死保护、回合末格挡与成长收益；救援药水仍由 `BotPotionPlanner` 在内核之外评估。内核失败或返回边界时回退到原 `TeamCombatPlanner`，不会每帧重试。
- 内核搜索在动作之间让出执行权（4ms 时间片，200ms 预算上限，深度9/宽度8/768节点），并对战斗对象、回合数、动作队列版本、集火目标和实况版本号做失效校验；过期或取消的计划清空首个动作，不部署。
- 已验证：跨 Bot 共享分支内 Bash/Strike 重排、所有者能量与遗物隔离、FeelNoPain/DarkEmbrace/Shuriken 等已导入卡遗物、抽牌链、NoDraw/消耗隔离、非法目标拒绝、回合结束与毒化分支保护；原生 FeelNoPain/DarkEmbrace 与四件遗物回调对照通过；47 个 Harmony 补丁挂载，既有协作/商店/事件/重连/节奏回归通过。
- 边界：内核尚未接入敌方回合、按角色结束回合、多人原生选牌分支和长期跨回合战略；遇到未完成选牌、回合结束请求或预测缺口时整场回退旧规划器。控制台测试只验证调用契约，**未做真实游戏动画、Steam 多机或 A10 胜率实测**。细节见 `src/CoopBots.Kernel/INTEGRATION.md`。
- 打包已包含 `CoopBots.Kernel.dll`，并在 `mod_manifest.json` 声明 `STS2-RitsuLib` 依赖；不随包分发任何第三方二进制。

## 0.13.0

CombatSolver 卡牌与遗物算法首批移植。安装包：`outputs/CoopBots-v0.13.0.zip`；游戏v0.111.0，所有真人一起更新。玩家建议保持关闭。

- 专属联动：Entrench、BodySlam、FeelNoPain、DarkEmbrace；打完牌后的格挡、消耗和抽牌会改变后续决策。
- 接入完整的当前支持范围内的动作链：Impervious、Backflip、Finesse、FlashOfSteel；包括消耗触发及已知抽牌序列。
- 遗物：Shuriken、Kunai、OrnamentalFan、Nunchaku；按持有者真实计数，计算后续力量、敏捷、格挡与返还能量。多段攻击只计一次出牌，重放分别计数。
- 新状态参与分支复制与去重，不修改真实计数；无新机制时复用空状态，保留现有搜索预算。
- 已取得移植授权（项目所有者确认），保留 `CoopBots/THIRD_PARTY_NOTICES.md`。移植细节：`outputs/port-preview20.md`；全方位借鉴计划：`outputs/combatsolver-adoption-plan.md`。
- 尚未移植全部卡牌与遗物，也未验证真实A10胜率。整手消耗、弃牌循环、充能球、召唤与随机降费待后续批次。

## 0.12.1

沙虫流沙即死决策修复。安装包：`outputs/CoopBots-v0.12.1.zip`，对应游戏v0.111.0；所有真人请一起更新。玩家建议保持关闭。

- 识别敌方回合开始的流沙强制击杀，格挡、回血和虚弱不再被当作吞噬的解法。
- 将 FranticEscape 纳入团队搜索及已知抽牌分支，分别计算每名玩家的倒计时；读取卡牌当前费用。
- 优先确认击杀解除威胁，无法击杀时规划逃脱；倒计时接近结束时给安全余量有限价值，充裕时继续输出。
- 修正防御药水的虚假救命评分；伤害药水击杀流沙来源可救命。当前暂不组合搜索“先喝防御药水再逃脱”的复杂救援。
- 依据游戏规则独立实现，未复制 CombatSolver 模拟核心。详见 `outputs/monster-hazards-preview19.md`；本轮未覆盖全部怪物或真实A10通关测试。

## 0.12.0

首批已知回合边界与延迟收益。安装包：`outputs/CoopBots-v0.12.0.zip`，对应游戏v0.111.0；所有真人请一起更新。玩家建议保持关闭。

- 镀层在本回合结束时才提供格挡，并处理BeaconOfHope分享；即时复制牌不能使用这些尚未获得的格挡。救援药水判断同步计入这些已知保护，避免重复花药。
- 恶魔形态投影到下回合获得力量，仪式纳入已知回合末力量；死亡角色不获得下一回合收益。新增投影的恶魔形态不再依赖通用能力加分。
- 壁垒评价承伤后能带到下一回合的剩余格挡，安全时可以投资保留格挡，危险时继续优先救援。
- 保留已知回合边界资源的有限收益分，不把未来真人行动或估计成长当作确定斩杀。尚未实现完整敌人状态机和下一回合手牌搜索。
- 原生Plating/Ritual/DemonForm/Barricade能力回调及格挡保留规则对照通过，新增战术与药水回归通过。

详情：`outputs/boundary-preview18.md`。

## 上一轮资源模拟（0.11.0）

首批资源模拟与战士启动连招。安装包：`outputs/CoopBots-v0.11.0.zip`，对应游戏v0.111.0；所有真人请一起更新。玩家建议保持关闭。

- 新增独立分支的手牌可用性、抽牌游标、手牌数量、禁抽牌、腐化、已弃置/消耗/能力牌状态，并纳入状态去重。只提交第一张真实合法牌，实际行动后重算。
- BattleTrance、Offering、PommelStrike、ShrugItOff可以按Bot当前抽牌堆的真实顺序，展开最多6张已支持卡牌；不再仅靠抽牌固定加分。此信息模式会读取普通玩家未必知道的抽牌顺序，尚不模拟洗牌RNG。
- Offering的回能与抽牌可连接后续攻击，保留支付致死生命代价的禁止条件。Corruption可在搜索中把后续技能费用降为0，并记录技能消耗，比较先启动再连续出牌的收益。
- 处理先出牌腾出手牌位置、10张上限和BattleTrance抽牌后禁抽。遇到洗牌、未支持抽到的牌或未适配抽牌Hook时停止精确展开，安全情况下保留原有单步回退。
- 修正DemonForm被当成本回合立即加力量的问题；原生抽牌顺序/数量/上限和腐化费用对照通过，新增连招与分支隔离回归通过。尚不是完整多回合模拟，也未验证A10胜率。

详情：`outputs/resources-preview17.md`。

## 上一轮持续集火（0.10.2）

修复随敌人当前攻击意图反复转火的问题。安装包：`outputs/CoopBots-v0.10.2.zip`，对应游戏v0.111.0；所有真人请一起更新。玩家建议保持关闭。

- 新增战斗内共享软集火目标，优先推进同一敌人的减员；攻击/异常状态意图轮换本身不再改变目标。
- 自动目标根据当前HP和格挡估计剩余击杀工作量，提交攻击后记录，跨回合保留；搜索和药水推演不修改目标记忆。新战斗独立，读档后从当前受伤情况重建。
- 目标死亡/不可攻击、另一目标显著更易击杀时重新选择；手动集火优先，救援和更高团队收益仍可覆盖软偏好。
- 固定双敌交替意图回放：两名Bot每轮各打一张Strike，每只敌人36HP。新版第3回合首次击杀、第6回合清场；0.10.1同场景首次击杀在第4回合。该受控回放不包含完整异常状态效果，不等于整局胜率测试。

详情：`outputs/focus-preview16.md`。

## 上一轮强度修复（0.10.1）

首轮强度修复。安装包：`outputs/CoopBots-v0.10.1.zip`，对应游戏v0.111.0；所有真人请一起更新。玩家建议保持关闭，沿用上一轮搜索缓存与性能优化。

- 未知能力不再直接禁止整名Bot连续出牌；尚未适配的伤害仍只作为估计，不能伪造确定击杀。已核对的DemonForm、Ritual、Barricade、Plating、Corruption状态不再取消普通攻击的确定性；其全部跨回合效果仍未完整模拟。
- 非致死战损可以和进攻、已建模成长收益权衡；低血量增加损血代价。真人死亡、团灭和确定战斗胜利仍保留优先顺序。搜索保留与最终选择使用同一评分比较，减少中途错误剪枝。
- 区分可进入投影和效果覆盖完整的牌。没有可识别投影效果的能力牌不再凭通用Power加分制造“团队最优”，而由单步回退评估。团队无方案时单独扫描部分支持的合法牌，避免被已建模牌的个人高分挡住；有人面临致死伤害时不启用这一推测回退。
- 新增健康/残血减伤取舍、有效防御、低成本救援、成长、未知能力下多牌序列、确定击杀边界和效果回退回归。仍未验证完整A10胜率。

详情：`outputs/strength-preview15.md`。

## 上一轮搜索性能优化（0.10.0）

首轮搜索性能优化。安装包：`outputs/CoopBots-v0.10.0.zip`，对应游戏v0.111.0；所有真人请一起更新。

- 玩家建议仍全部关闭；Bot协作所用的真人手牌评估保留。
- 每个完成扩展的节点只计算一次评分；普通卡不再额外复制一次状态；状态去重改用结构化哈希并核对字段，保留原有精度和稳定选择顺序。
- 伤害和格挡计算在单次同步搜索内按完整调用输入缓存；下一次搜索重建，避免沿用真实行动前的结果。无格挡和无分享时减少临时分配。
- 单次搜索超过100ms时记录搜索类型、耗时、扩展节点、评分缓存命中、伤害Hook调用、格挡投影和分配字节数，日志最多每5秒一次。该记录覆盖搜索本身，不是完整游戏帧耗时。
- 新增四人20张手牌、3敌人的重复搜索基准，检查结果稳定和真实资源不被修改。初步对照中搜索中位耗时约237ms降到50ms、单次分配约237MiB降到50MiB；这是无画面模型测试，不代表实际帧率或A10胜率。

本轮保持搜索深度、评分目标和卡牌覆盖不变。后续继续处理未知能力限制连招、效果支持分级和非致死战损的过高优先级。

## 上一轮建议关闭（0.9.2）

暂时关闭全部玩家建议的计算与显示，包括战斗出牌/状态铺垫、路线、选牌和遗物建议，以减少主线程重复规划。Bot团队决策、读取真人手牌的协作评估、暂停和集火控制继续保留。关闭状态由统一代码开关控制，暂不提供游戏内开启入口。

安装包：`outputs/CoopBots-v0.9.2.zip`，对应游戏v0.111.0；所有真人请一起更新。本次仅移除建议计算开销，Bot自身搜索仍可能引起卡顿，实际改善幅度需要游戏内测试。

## 上一轮临终策略修复（0.9.1）

新增临终贡献与高代价救援比较。安装包：`outputs/CoopBots-v0.9.1.zip`，对应游戏v0.111.0，包含商店及此前修复；所有真人请一起更新。

- 真人死亡、团灭仍优先避免。AI死亡改为有限的未来贡献损失：每名AI按40+生命上限的25%计入代价，再加全队实际掉血；不再仅因为少死一名AI就无条件投入团队资源。当前主要用团队战损衡量救援代价，不是完整的胜率或后续回合求解。
- 在预测死亡的方案中，不再给该角色无法利用的未来自身力量、敏捷、专注或能力牌成长加分，也不让其个人评分推动无效防御。已在本回合兑现的伤害、存活队友的增益及格挡分享仍计入收益。
- 防御/回血药水即使单独不能救活，也会先用投影检查配合后续牌是否能救下目标。联合方案仍救不活时不再为单纯缩小致死缺口用药。回血与格挡使用独立投影，不能把回血误当作Mimic可复制的格挡。
- 补充常规伤害状态下新挂虚弱的保守逐击伤害估计，避免把虚弱配合格挡的可救援局面误判为必死。未知伤害能力仍保守处理。
- 每次实际行动后重新计算，没有永久的“放弃某个Bot”标记；不会为所谓临终贡献假定AI支付致死生命代价后仍能完成动作。

新增回归覆盖临终输出、援护增益、无效自身成长、无效药水与联合抢救、回血/格挡区别、Beacon分享、虚弱自救、昂贵救援取舍及真人保护。构建与既有测试通过；没有实际多机或A10通过率测试。详情：`outputs/last-stand-preview12.md`。

## 上一轮商店修复（0.9.0）

新增Bot商店购物、金币预算与团队遗物估值。安装包：`outputs/CoopBots-v0.9.0.zip`，对应游戏v0.111.0，包含此前所有修复。**本版新增联机消息，所有真人必须使用同一版本。**

- 主机为每个Bot读取其独立货架和余额，比较买牌、遗物、支持的药水、删牌与留钱。默认保留25金币，高收益购买允许突破；价格超过估计收益时保留金币，不强行清空余额。当前为逐笔贪心启发式，并非整局最优预算。
- 药水槽满时不买；低血量提高已支持药水的购买价值。删牌优先可移除诅咒，仅在牌组至少12张时考虑未升级基础牌。购买会员卡按剩余预算和有用库存估计回本；MawBank未失效时计入终止收入的机会成本。
- 每笔交易由主机发出带Bot身份、房间位置、序号、价格和购买前状态的指令；各端执行同一交易，核对余额、牌组、遗物、药水和库存后确认。重复指令不会再次扣钱；不使用或改写真人的金币。Bot投票离店前等待交易确认。
- 原生购买仍负责商品效果、补货和购买后钩子；异步交易范围内抑制原生“本地玩家领取奖励”广播。Bot会员卡绕过仅限本地真人的折扣限制，每笔交易固定购买前价格，完成后重新读取库存价格。每Bot每店最多8笔，防止补货循环。
- 遗物新增BagOfMarbles、RedMask、MassiveScroll与经济遗物的估值。共享减益参考团队攻击储备、人数及已有同类遗物/常见铺垫牌；Vajra考虑已识别多段攻击，敏捷遗物考虑格挡分享/复制机会。商店、宝箱和真人建议共用遗物评分。
- 路线建议新增各成员独立购物预算人数，不把全队金币合并成可支付余额。

测试通过实际买牌、买药水、买遗物、付费删牌、删牌涨价、会员卡购买前后价格、身份/余额/库存校验、序列化和独立对局状态重放。当前共47个Harmony补丁。**尚未进行Steam多机、网络延迟/掉线恢复及A10胜率实测。** 若交易不一致则停止购物与Bot离店投票并记录日志，不会在部分失败后盲目重买；中途断线可能需要恢复连接或重新读档。详细边界见 `outputs/shop-fixes-preview11.md`。

## 上一轮石板事件修复（0.8.2）

修复真理石板（TabletOfTruth）的连续生命上限牺牲决策，安装包：`outputs/CoopBots-v0.8.2.zip`，包含此前多人卡牌修复，对应游戏v0.111.0。所有真人请一起更新。

- 所有难度Bot采用同一事件生存策略，绕过原先关键词同分时默认继续及低难度随机分支。
- 读取实时生命上限代价；只考虑3、6点的低价解读，拒绝12、24点及降至1上限的选项。累计支出不得超过9点且不得超过推算入场上限的15%。
- 支出后上限不得低于40或角色初始上限70%中的较大者；当前血量不超过上限一半时退出。
- 每次重新检查可升级牌，按随机升级候选池的平均启发式价值比较永久损失、当前生命截断损失及初始页治疗收益。没有可升级牌不再付费；初始页受伤时可以选择恢复20生命。
- 健康80上限战士、基础可升级牌充足的回归局面，选择前两次后在71上限停止。已在59/35上限的存档会选择退出，不会恢复此前已经损失的上限。
- 决策无临时次数缓存，依照事件原版价格序列推算累计支出；读档及不同设备使用同一状态得到相同结果。异常价格保守退出，退出按钮不可用时等待。

新增测试覆盖所有Bot难度、连续价格阶段、低血量、低上限、治疗、无可升级牌、按钮顺序变化及重置。日志：`outputs/build-preview10.log`。策略是保守启发式，并非精确的随机升级或整局胜率求解；未实测Steam多机或A10通过率。

## 上一轮多人卡牌第二批（0.8.1）

多人触发与连招建模第二批，安装包：`outputs/CoopBots-v0.8.1.zip`，对应游戏 v0.111.0。包含0.8.0的修复；所有真人请一起更新。

- TagTeam：按施加者记录追加出牌效果，下一张其他队员攻击消耗效果并重复整张牌。多段攻击整组重复，群攻可消耗多个敌人的标记；追加出牌不再次支付能量/星星，每次出牌独立结算已建模效果。
- GangUp：读取当前真实攻击历史，再增加本条计划中其他队员对同一敌人的已确认命中次数；升级版读取每击7点的成长。不会把自己的攻击计入。
- OneForAll：全队后续零能量费用攻击逐击获得增伤；付费攻击不获益，升级版使用4点。已有能力由游戏伤害钩子计算，新打出的能力只追加增量。
- Sneaky：其他队员每次打出攻击牌提供一次被动格挡，多段攻击不重复触发，TagTeam的追加出牌可再次触发。
- BeaconOfHope：将后续获得格挡的一半分享给队友，按游戏顺序处理多个Beacon的递归保护和被动格挡修正。NoBlock不阻止这种非卡牌的被动格挡。
- 搜索不再对已经确认死亡的目标继续安排单体攻击；重复出牌和分享链设有保守上限，防止极端叠层拖慢决策。

新增自动化回归覆盖上述五张牌、升级数值、同施加者排除、人工制品、群攻追加、逐次Bash结算、一次性消耗和双Beacon递归边界。详细说明及剩余机制见 `outputs/multiplayer-fixes-preview9.md`。这些是模型测试，尚无Steam多机或A10胜率实测。

## 上一轮多人卡牌第一批（0.8.0）

多人卡牌建模第一批修复，安装包：`outputs/CoopBots-v0.8.0.zip`，对应游戏 v0.111.0。所有真人请一起更新。

- 搜索逐人扣除星星；Constellation 不再规划无法支付的第二张。后续候选允许当前仅因资源不足而不能出的牌，首个动作仍须通过游戏实时合法性检查。搜索不修改游戏真实资源或状态。
- Rally 按每个存活队员计算格挡及其修正；Mimic 区分格挡参考对象与实际受益者；Mimic、DemonicShield 可以复制本条计划前面产生的格挡。
- BelieveInYou、Constellation 给实际目标回能，EnergySurge 给全队回能，搜索可继续展开队友原本付不起的攻击。HuddleUp、Constellation 的抽牌进入收益评估，但不会假设抽到某张指定牌。
- 计划中的力量影响已支持直接攻击的每一击，敏捷影响施法者后续格挡；当前回合中临时增益可用于连招，尚不推演跨回合到期。
- 易伤后的直接攻击按逐击取整计算，已支持攻击不再因挂了易伤而丢失确认伤害。规划消耗人工制品层数，夹击、击倒按施加者分别计算，排除施加者自己的受益；升级击倒读取真实倍率。未知/复杂攻击仍只作为估算。
- Cacophony 不再把延迟触发的66伤害当成立即伤害；尚未模拟它的累计抽牌触发。

本轮修复范围与剩余工作见 `outputs/multiplayer-fixes-preview8.md`。TagTeam 的追加出牌、持续触发团队能力、生成/传递卡牌、召唤物和充能球等仍需后续建模；37张审计表是修复前记录，不能视为本版覆盖清单。当前实现仍是有限搜索，不保证全局最优或A10通过率。

## 上一轮团队统筹者（0.7.0）

新增团队统筹者，安装包：`outputs/CoopBots-v0.7.0.zip`，对应游戏 v0.111.0。所有真人请一起更新。

统筹者采用反应式选择树，每次行动重新选择分支：救援药水 → 团队计划 → 未覆盖卡牌/异常回退 → 等待。救援药水与团队计划按预计避免死亡的优先级比较；同等救命效果优先用牌保留药水。团队方案不再加入个人分数排行榜参与竞争，也不会在团队决定等待时被普通个人行动覆盖。

- 团队 Beam 搜索覆盖所有难度 AI；难度仍影响个体回退和部分非战斗选择。最终排序优先死亡风险、确认胜利、全队总掉血，再比较击杀、威胁、协同等。真人死亡仍有更高保护权重，但非致命战损不再只偏向某个成员。
- 允许已建模的生命代价换取全队净收益。例如 DemonicShield 让施法者掉 1 血、避免队友掉 10 血，计为全队少掉 9 血；不会为减少非致命掉血而让施法者死亡。候选裁剪优先保留援护牌。
- 真人出牌建议也调用同一团队规划器，约束首个动作必须属于当前真人，再推演 AI 后续配合。其他真人尚未执行的动作仍仅作为低置信度机会，不计入确认伤害。建议不自动执行。
- 选牌使用团队分工：按稳定 Bot ID 确定一名优先补援护的队友，其他队友兼顾输出与辅助；重复援护牌收益递减，补充常见易伤/虚弱牌。真人选牌建议共用同一估值入口。为避免联机并发领奖顺序影响结果，Bot 的这部分估值使用稳定名单和自己的牌组，不读取另一 Bot 正在领取的奖励；目前不是完整的跨角色构筑规划。
- 宝箱对尚未投票的 AI 枚举不冲突分配，最大化现有遗物适配估值之和；锁定真人和已投票 AI 的选择，每次投票后重算。不再出现几个 AI 都选同一个遗物、其他遗物无人选择的分配行为。
- 保留正常 1 秒、真人全结束后 0.2 秒的全队行动节奏及暂停/集火控制。面板显示决策分支及计划预计减少的全队战损。

边界：统筹者仍使用当前回合有界搜索和有限卡牌语义，不是全游戏精确求解器。Intercept、复杂 X 费及部分特殊牌仍走回退策略；遗物适配沿用已支持的七类专属估值，未知遗物仅按中性价值分配，不能声称全池最优。路线建议仍为原有局部启发式。药水尚未与整条出牌序列联合展开。

新增回归：普通 AI 非致命牺牲净省 9 血、拒绝致死牺牲、个人极高分不能覆盖团队方案、团队等待不被个人牌覆盖、援护选牌/重复递减、Anchor/Vajra 全队适配分配、真人保留项和无遗物时跳过。Release、36 个 Harmony 补丁及既有协作/节奏/重连测试通过；未做真实 Steam 多机或 A10 胜率实测。

## 上一轮自主出牌与自动加速

本轮改为自主出牌与自动加速。安装包：`outputs/CoopBots-v0.6.0.zip`，对应游戏 v0.111.0。以下规则取代历史版本的“等待真人放行/仅铺垫先手”机制。

- 默认 AI 全队共用一个节奏：约每 1 秒提交一次行动（出牌或药水），不需要真人先打牌或点击放行。开始新的战斗回合时也重新计时。
- 所有存活真人点击“结束回合”后，自动改为每 0.2 秒一次行动，快速出牌并依次结束 AI 回合。空手或零能量不等同于真人已结束；只结束一个真人的回合不会触发多人加速。真人撤销结束后恢复正常速度。
- 每次仍只提交一个动作，等待当前动作和队列处理完成，再按实际战场重新规划；动画、选择和网络延迟可能让实际速度慢于上述间隔，不加速游戏动画，也不一次塞入多张牌。
- 暂停同时阻止普通模式和快速模式。移除旧放行按钮，保留暂停/继续与集火；界面显示当前节奏。
- 真人尚未结束时，AI 无可用行动会保留回合，允许后续抽牌、回能等真人援助带来新的行动机会。真人全部结束后才集中收尾。
- 地图、事件、宝箱选择与原有多真人同步策略保持原有流程；多人联机请所有人一起更新到此版本。

新增自动测试覆盖 1 秒自主行动、已有冷却即时缩短至 200ms、多人结束判定、死亡真人不阻塞加速、暂停、长时间停顿后不突发补发、回合与读档计时重置。Release 构建与 36 个补丁挂载、既有协作/重连回归通过。尚未做本版游戏内动画节奏和真实 Steam 联机实测。

## 上一轮多真人协作

本轮检查并修复多真人协作。安装包：`outputs/CoopBots-v0.5.2.zip`，针对游戏 v0.111.0。所有真人须使用同一游戏版本、相同版本的本 Mod 及其他影响玩法的 Mod。总人数仍按原版大厅最多四席：例如 2 真人 + 2 AI、3 真人 + 1 AI。

- 战斗/药水/地图/宝箱动作只由房主提交，客户端按原版消息重放。Bot 的选牌、奖励、个人事件与休息点自动处理仍需各端安装本 Mod，在本地执行相同策略；因此不支持仅房主安装。
- 修复客户端不显示战斗/路线/宝箱建议、建议错误指向第一位真人的问题；每端建议使用其本地玩家。客户端可看建议，暂停/集火仍由房主控制。
- 多真人时房主按钮变为“我已准备好配合”，只替房主本人确认；其他活着的真人需结束自己的回合，AI 才开始不受铺垫限制的输出。易伤/虚弱铺垫仍可提前发生；规划和救援同时考虑所有真人。
- 地图和共享事件等待所有真人投票后，按真人意见的近似比例分配 AI 票。2 真人分歧 + 2 AI 时各跟随一票；3 真人中有 2 人同意时，1 AI 跟随多数。不再让所有 AI 固定跟随第一位真人。不可整除的票数按稳定席位顺序处理余数；最后选择仍由游戏原版投票机制决定。
- 任意真人改票都会清除旧 AI 跟随票，随后重新分配；宝箱仍避开所有真人已选的遗物。

测试新增 2 真人 + 2 AI 模型场景、3 真人投票分配、客机手牌协同与救援、真人改票、同步屏障不代填真人状态，以及房主动作在客户端以相同 AI 身份和动作编号只入队一次、不再广播的消息接收测试。测试使用模拟网络服务，不是双电脑 Steam 实机联机；UI、完整事件/奖励/休息点同步、延迟/掉线竞争及长局稳定性仍需实测，不能据此保证所有组合都无失步。

## 上一轮读档恢复

新增退出读档后的 AI 自动就位。安装包：`outputs/CoopBots-v0.5.1.zip`，对应游戏 v0.111.0。

- 从游戏的多人继续/读档入口加载原存档，读档大厅会自动补齐存档中的 Bot，并设为已准备。真人正常点击准备即可继续，无须重新添加 AI。
- 使用存档原有的 Bot ID（含难度和编号），不创建新角色、不修改原有牌组、遗物、血量或存档格式。已有本 Mod 的旧存档可按相同身份恢复。
- 读档大厅与运行中大厅均补齐虚拟队友，重复恢复不会增加重复席位；真人加入时通过原版消息获得完整名单，虚拟席位不接收物理网络包。
- 不自动准备真人、不把断线真人改成 Bot；主机网络已断开时停止提交 Bot 行动。恢复进度仍以游戏实际保存的内容为准，不增加战斗中任意时刻存档或房主迁移功能。

验证：36 个 Harmony 补丁挂载；真实 LoadRunLobby 的房主读档、准备并开始流程；模拟网络的真人加入握手；名单幂等、原 ID/血量保留、客户端名单、重复进入与无 Bot 存档。Release 构建与原有协作回归通过。未进行真实 Steam 断网重连和游戏内完整读档实测。

## 上一轮真人协作优化

本轮新增真人协作与建议，安装包：`outputs/CoopBots-v0.5.0.zip`（游戏 v0.111.0）。

- 真人尚未结束回合时，Bot 可先打有即时团队收益的状态牌。目前明确支持 Bash、Uppercut、Shockwave、Neutralize、LegSweep、SuckerPunch、GoForTheEyes；普通输出仍等真人点击“你们先打”或结束回合。暂停同时禁止铺垫行动。
- 易伤铺垫参考队友（含真人）当前合法可支付的攻击；虚弱参考当前实际来袭伤害。已有状态、Artifact、没有收益、已知致命反伤和会直接击杀目标的铺垫攻击会保守跳过。不覆盖所有卡牌与复杂遗物触发。
- 战斗面板建议真人当前可打的牌与目标，包括先施加状态；读取真人当前手牌、费用、能量和战场，每 500ms 空闲时刷新。每个 Bot 动作只提交一张牌，下一步重新读取真人行动后的实际局面，不执行旧的多步脚本。
- 地图展示基于队伍最低血量、真人金币及后续一步的路线建议，Bot 仍跟随真人投票。选牌奖励界面展示基于当前牌组结构的建议；遗物选择界面及宝箱展示已支持遗物的条件估值。
- 当前遗物专属建议覆盖 Anchor、BagOfPreparation、Vajra、OddlySmoothStone、Lantern、BloodVial、HornCleat。未识别遗物明确提示无法可靠估值，不能把其零分理解为价值低。
- 宝箱 Bot 等真人先表态，并避开真人已选遗物；没有其他可选项则跳过投票争抢。

建议是启发式估计，不保证全局最优，不自动操作真人。选牌尚未完整模拟构筑组合、移除/升级/事件选牌与跳过收益；路线尚未完整搜索整幕；遗物尚未覆盖全池。原有天才团队 Beam 规划保留。

本轮验证：Release 构建、32 个 Harmony 补丁挂载、真实游戏模型场景回归（提前铺垫、状态去重、真人能量变化、Artifact、普通攻击不得抢跑、虚弱与群体状态、真人建议合法性、低血路线与遗物估值）。尚未进行本版本的游戏内 UI、完整联机或 A10 胜率实测。

建议实测顺序：真人保留攻击观察 Bot 先挂易伤；真人自己先挂易伤确认 Bot 不重复挂；点击暂停确认铺垫也停止；消耗能量/击杀目标确认建议更新；打开地图、奖励与宝箱检查提示显示及选择权。

## 保留的第二阶段能力

第一阶段安装包保存在 outputs/CoopBots-0.3.0.zip。
当前预览版包含第一阶段功能，并继续第二阶段：
- 战斗面板新增“集火”目标选择；危险救援优先于集火偏好。
- “天才”Bot 使用有界团队 Beam Search，同一节点分别记录每个 Bot 的能量、已用手牌、敌人硬/软血量、队友格挡、生命代价、易伤、虚弱与行动顺序。
- 中间保路同时保留生存、确认击杀、集火、成长铺垫和真人协作路线；最终按“团队死亡 → 确认胜利 → 团队战损 → 确认击杀 → 威胁/敌方生命 → 协作与成长”分层选择，不用一个总分掩盖生存问题。
- 伤害分为“可确认结果”和“策略估计”：只有明确支持的直伤牌和透明状态才能声明联合击杀；复杂 Power、延迟伤害和计划中的易伤只影响软估值。
- 真人当前手牌会形成低置信度的攻击、格挡和增益协同潜力：例如提高 Bot 先上易伤或给真人加力量的价值，也会减少无威胁目标的重复过量投入。
- 真人潜力永远不会计入确认斩杀或确认保命；玩家结束回合后，其手牌潜力立即从规划中移除。
- 只执行计划的首个动作，下一步读取真实状态重新规划；不把真人尚未执行的行动算进击杀。
- 通用动态变量用于软规划，明确支持的基础直伤牌用于硬确认；X 费、星星 X 费、Bullet Time、Double Energy、Intercept 和复杂一次性 Power 交回单 Bot 策略或限制为保守边界。
- 真人修改/撤回选择时，在游戏统计全部票之前清除旧的 Bot 跟随票。

这仍是当前回合的轻量团队规划器，不是 CombatSolver 的完整跨回合模拟器。跨角色供能、随机抽牌展开、全卡池精确语义和完整药水联合搜索尚未完成。
最新测试额外覆盖团队 Beam 生存优先、联合击杀资源分配、资源变化后硬斩杀失效、真人手牌软协同、真人伤害不得伪造确认斩杀、NoBlock 对援护的影响、危险 Intercept 拒绝及共享事件旧票作废。
所有自动测试在独立进程运行，不替代实际游戏中的面板布局、完整联机及 A10 胜率测试。

## CombatSolver 参考边界

本版本参考 `CombatSolver-0.33.9` 将候选展开、中间 Beam 保路、状态去重和最终路线排序分离的架构思想，并针对多人状态独立实现团队投影。没有复制 CombatSolver 的模拟引擎源码，发布 DLL 不依赖或包含 CombatSolver；参考项目自身也明确说明目前没有覆盖整个仓库的统一软件许可证。
# Co-op Bots 0.3.0 — 第一阶段协作版

本版本针对游戏 v0.111.0 构建；不再声称兼容 v0.107.1。主要体验目标为 1 真人 + 3 Bot。

## 安装
退出游戏，将发行包里的 CoopBots 文件夹放到游戏可执行文件旁的 mods 目录，例如：
B:\SteamLibrary\steamapps\common\Slay the Spire 2\mods\CoopBots
其中必须有 CoopBots.dll 和 mod_manifest.json。不要放进 Steam 工坊根目录。
真实联机玩家必须安装同版本。旧版目录应放到 mods 外备份，避免重复加载。

## 战斗控制
每回合默认等待真人。准备好后点击右上角“你们先打（本回合）”，或结束真人回合。
“暂停”阻止后续 Bot 行动提交，已在执行的动画/行动不会撤销。“继续”恢复之前的状态。
放行只作用于当前回合；下一回合重新等待。真人死亡后存活 Bot 会继续完成战斗。
Bot 在整个团队没有可用的正收益行动前不提前结束自己的回合，便于等待辅助。
默认出牌间隔从 380ms 调整为至少 900ms（还需等待游戏行动结算）。

## 第一阶段变化
- 地图及共享事件跟随首位真人玩家；有多名真人时先等所有真人投票。
- 从全部 Bot 当前候选中选择行动，易伤收益考虑其他 Bot 的可支付攻击。
- 援护按实际受益者计算受伤和格挡；Intercept 考虑施法者承担的额外伤害。
- 修复 Vicious 条件抽牌误判和 FightMe 敌方加力量的代价遗漏。
- 危险时使用 Bot 自己的 BlockPotion、BloodPotion、FirePotion、ExplosiveAmpoule、WeakPotion；目标可包含真人。
- 保留原生药水回调、排队状态和 Bot 网络身份；不会代用真人药水。
- 按战斗清理计数；错误限频记录；持续失败暂停并保留人工干预机会。

## 构建与验证
Windows：运行 scripts/build.ps1，可传 -GameData 和 -Dotnet。
需要 .NET 9 SDK。游戏 DLL 是运行依赖，不随发行包分发。
测试涵盖 Harmony 挂载、控制许可生命周期、卡牌基础分析，以及使用原版模型的援护、药水救援/节约、跨 Bot 易伤场景。
独立测试进程替代了 Godot 原生打印、存档入口和战斗进行状态；不访问玩家存档。
测试不等同于实机 UI、完整联机、完整卡池或 A10 通关率验收。

## 已知边界
药水及伤害预估属于首轮保守启发式，并非全规则模拟；特殊敌人、触发能力、随机伤害及其他 MOD 可影响准确性。
商店、长期构筑、全部药水、复杂角色机制属于后续阶段。
第一阶段没有达到或验证 A10 60% 通关率。安装后建议新建测试局，重点检查：真人结束回合后推进、暂停/继续、路线跟随、危险局面的援护和药水。

---

## 原 0.2.0 项目说明（历史记录，兼容性与构建步骤以上文为准）
# Slay the Spire 2 联机机器人 Mod

`Co-op Bots / 联机机器人` 是为《Slay the Spire 2》官方 Steam 版本编写的代码 Mod。`0.1.1` 修复开局黑屏；`0.1.2` 修复地图投票；`0.1.3` 修复怪物回合同步；`0.2.0` 重新设计“天才”战斗策略，并把大厅玩家结构改为主分支/Beta 双版本自适应。

## 功能

- 房主可在标准多人角色选择大厅中添加或移除机器人。
- 原版 4 人席位内可添加 1–3 个机器人；例如 1 真人 + 3 Bot、2 真人 + 2 Bot。
- 每个机器人添加时可独立选择角色和难度。
- 四档行为不是属性倍率，而是决策质量：
  - **蠢**：确定性乱选，可能提前结束回合，也可能跳过卡牌奖励。
  - **普通**：按卡牌类型、基础伤害和格挡进行简单决策。
  - **聪明**：考虑费用、0 费牌、目标剩余生命、卡牌稀有度和路线风险。
  - **天才**：读取实时敌人意图和卡牌预览值，进行斩杀、威胁、生存、出牌顺序与资源规划。
- 战斗动作由房主通过游戏原生动作队列同步，不伪造 Steam 连接。
- 自动处理地图投票、事件选项、卡牌/遗物奖励、手牌/牌组选牌及休息点，避免等待不存在的远程玩家。
- Bot 名称和难度会显示在大厅及游戏内玩家区域。

## 安装

所有参与联机的真人玩家都必须安装**完全相同版本**的 Mod，否则官方的 Mod 版本校验可能拒绝连接。

macOS 本地 Mod 目录：

```text
SlayTheSpire2.app/Contents/MacOS/mods/CoopBots/
  CoopBots.dll
  mod_manifest.json
```

把发布包中的 `CoopBots` 文件夹复制到上述 `mods` 目录，然后通过 Steam 启动游戏。首次使用 Mod 时，游戏会显示官方 Mod 提示/确认界面。

## 使用

1. 进入“多人游戏”，由一个真人创建标准游戏大厅。
2. 在角色选择页面右上角找到“联机机器人 / CO-OP BOTS”。
3. 选择机器人角色、难度，点击“添加机器人”；可重复添加到大厅满员。
4. 需要给真人腾位置时，点击“移除最后一个”。
5. 真人玩家都准备后开始游戏，机器人默认已准备。

## 0.2.0 天才策略

- 每次行动都重新读取敌人意图、生命/格挡、Bot 当前格挡、能量和整手牌，而不是只按卡牌稀有度排序。
- 先检查单卡斩杀、整手牌近似斩杀和 AOE 多杀；可击杀正在攻击的敌人时，把击杀同时视为输出与减伤。
- 按敌人威胁集中目标，避免平均撒伤害；立即处理高伤攻击者，并降低无意义清理次要召唤物的优先级。
- 只为尚未覆盖的来袭伤害获取格挡；零来袭或已有足够格挡时允许保留能量并结束回合。
- Weak 只优先施加给正在攻击的敌人；Vulnerable 会根据后续可打出的攻击价值前置。
- Adrenaline、Offering、抽牌和即时能量牌优先展开手牌；Double Energy、Bullet Time、X 费牌有独立顺序规则。
- Strength、Dexterity、Focus 与 Power 在长战斗/早期回合提高优先级；本回合可斩杀时降低慢速成长牌优先级。
- 识别多段攻击、AOE、Poison、Doom、HP 代价与 Hyperbeam 的 Focus 损失；低血时不会为小收益盲目卖血。
- 日志会记录每次天才 Bot 的评分与理由，便于继续针对具体误操作调优。

策略参考了社区常用的几条高水平原则：先检查斩杀、击杀攻击者等价于减伤、长战斗才投资成长、抽牌用于寻找当回合答案，以及不要在无来袭伤害时机械叠格挡。参考：[Slay the Spire 2 社区综合攻略](https://www.reddit.com/r/slaythespire/comments/1s63nb4/slaying_the_second_spire_a_vaguely_uncrunchy/)、[新玩家战斗建议](https://www.reddit.com/r/SlayTheSpire2/comments/1ryw5hz/tips_for_a_new_player/)、[Steam 社区策略指南](https://steamcommunity.com/sharedfiles/filedetails/?id=3704645422)。

## 当前边界

- 发布 DLL 以 Steam 默认主分支 `v0.107.1` 为最低 API 基线，并已在公开测试分支 `v0.111.0` 的实际程序集上完成跨版本加载及 27 个 Harmony 补丁冒烟测试；大厅玩家结构会在运行时自适应两种版本。
- 当前只支持原版最多 4 个总席位；未声明兼容扩人数 Mod。
- Bot 只在开局大厅中添加，暂不支持中途把掉线真人替换为 Bot。
- 商店首版采取保守策略：Bot 不主动购买，但不会阻塞房间推进。
- 含 Bot 的存档跨会话恢复尚未纳入首版验证；建议一次联机局内完成或保留存档备份。

## 开发与验证

项目使用 `.NET 9`，直接引用官方游戏程序集。执行：

```bash
./scripts/build.sh
```

构建会运行两个门槛：

1. Release 编译必须零警告、零错误。
2. Harmony 冒烟测试必须成功挂载大厅、战斗循环、选牌、奖励、事件相关补丁，并验证 Bot ID/难度编码往返。

源码入口：`src/CoopBots/ModEntry.cs`。发布物生成到 `outputs/CoopBots-0.2.0/` 和对应 zip。



当前联合规划会保守跳过一次性增益、全局费用变化及未识别能力，避免重复计入 Vigor/免费攻击等收益；伤害按每次命中取整。

---

# 附录：版本号重排对照表

2026-09-14 起，版本号由「单调递增的 preview 计数」改为按**改动性质**分档：

- `patch`：只修 bug / 调参 / 日志与埋点 / 文档
- `minor`：新能力、新子系统、架构改动，或一批成体系的行为改动
- `major`：重塑性改动或公开发布里程碑

历史编号全部重排；`1.0.0` 保留给正式公开发布（当前版本仍为私人可见，故按阶梯顺延为 `0.34.0`）。对照表如下，工坊页早期更新说明里的旧编号可在此查回。

| 旧编号 | 新编号 | 分档 |
|---|---|---|
| `v0.1.0` | `0.1.0` | — |
| `v0.1.1` | `0.1.1` | patch |
| `v0.1.2` | `0.1.2` | patch |
| `v0.1.3` | `0.1.3` | patch |
| `v0.2.0` | `0.2.0` | minor |
| `v0.3.0` | `0.3.0` | minor |
| `preview.1` | `0.4.0` | minor |
| `preview.3` | `0.5.0` | minor |
| `preview.4` | `0.5.1` | patch |
| `preview.5` | `0.5.2` | patch |
| `preview.6` | `0.6.0` | minor |
| `preview.7` | `0.7.0` | minor |
| `preview.8` | `0.8.0` | minor |
| `preview.9` | `0.8.1` | patch |
| `preview.10` | `0.8.2` | patch |
| `preview.11` | `0.9.0` | minor |
| `preview.12` | `0.9.1` | patch |
| `preview.13` | `0.9.2` | patch |
| `preview.14` | `0.10.0` | minor |
| `preview.15` | `0.10.1` | patch |
| `preview.16` | `0.10.2` | patch |
| `preview.17` | `0.11.0` | minor |
| `preview.18` | `0.12.0` | minor |
| `preview.19` | `0.12.1` | patch |
| `preview.20` | `0.13.0` | minor |
| `preview.21` | `0.14.0` | minor |
| `preview.22` | `0.14.1` | patch |
| `preview.23` | `0.14.2` | patch |
| `preview.24` | `0.15.0` | minor |
| `preview.25` | `0.15.1` | patch |
| `preview.26` | `0.15.2` | patch |
| `preview.27` | `0.15.3` | patch |
| `preview.28` | `0.15.4` | patch |
| `preview.29` | `0.15.5` | patch |
| `preview.30` | `0.15.6` | patch |
| `preview.31` | `0.15.7` | patch |
| `preview.32` | `0.16.0` | minor |
| `preview.33` | `0.17.0` | minor |
| `preview.34` | `0.18.0` | minor |
| `preview.35` | `0.19.0` | minor |
| `preview.36` | `0.19.1` | patch |
| `preview.37` | `0.20.0` | minor |
| `preview.38` | `0.21.0` | minor |
| `preview.39` | `0.21.1` | patch |
| `preview.40` | `0.21.2` | patch |
| `preview.41` | `0.22.0` | minor |
| `preview.42` | `0.22.1` | patch |
| `preview.43` | `0.22.2` | patch |
| `preview.44` | `0.23.0` | minor |
| `preview.45` | `0.24.0` | minor |
| `preview.46` | `0.24.1` | patch |
| `preview.47` | `0.24.2` | patch |
| `preview.48` | `0.25.0` | minor |
| `preview.49` | `0.25.1` | patch |
| `preview.50` | `0.25.2` | patch |
| `preview.51` | `0.25.3` | patch |
| `preview.52` | `0.25.4` | patch |
| `preview.53` | `0.25.5` | patch |
| `preview.54` | `0.25.6` | patch |
| `preview.55` | `0.26.0` | minor |
| `preview.56` | `0.27.0` | minor |
| `preview.57` | `0.28.0` | minor |
| `preview.58` | `0.28.1` | patch |
| `preview.59` | `0.29.0` | minor |
| `preview.60` | `0.29.1` | patch |
| `preview.61` | `0.29.2` | patch |
| `preview.62` | `0.30.0` | minor |
| `preview.63` | `0.30.1` | patch |
| `preview.64` | `0.30.2` | patch |
| `preview.65` | `0.31.0` | minor |
| `preview.66` | `0.31.1` | patch |
| `preview.67` | `0.31.2` | patch |
| `preview.68` | `0.31.3` | patch |
| `preview.69` | `0.31.4` | patch |
| `preview.70` | `0.31.5` | patch |
| `preview.71` | `0.31.6` | patch |
| `preview.72` | `0.32.0` | minor |
| `preview.73` | `0.32.1` | patch |
| `preview.74` | `0.32.2` | patch |
| `preview.75` | `0.32.3` | patch |
| `preview.76` | `0.33.0` | minor |
| `preview.77` | `0.33.1` | patch |
| `preview.78` | `0.33.2` | patch |
| `preview.79` | `0.33.3` | patch |
| `preview.80` | `0.33.4` | patch |
| `preview.81` | `0.34.0` | minor |
